# Environment Variables Template
# Copy to .env and fill in real values
# WARNING: Never commit real secrets! The pre-commit hook will block it.
# See: scripts/check_secrets.py

# Keystatic GitHub Mode Configuration (for Cloudflare Pages)
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set the callback URL to: https://your-cloudflare-pages-url.pages.dev/api/keystatic/github/oauth/callback

KEYSTATIC_GITHUB_CLIENT_ID=your_github_oauth_client_id
KEYSTATIC_GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
KEYSTATIC_SECRET=generate_a_random_secret_string_here

# Generate KEYSTATIC_SECRET with: openssl rand -hex 32

# Note: These are only needed for the Cloudflare Pages CMS deployment.
# Local development uses file-based mode and doesn't need these.
