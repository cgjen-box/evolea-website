---
phase: 01-foundation-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/fonts/Fredoka-SemiBold.woff2
  - public/fonts/Fredoka-Bold.woff2
  - public/fonts/Poppins-Regular.woff2
  - public/fonts/Poppins-Medium.woff2
  - public/fonts/Poppins-SemiBold.woff2
  - src/styles/global.css
  - src/layouts/Base.astro
autonomous: true
requirements: [PERF-05]
must_haves:
  truths:
    - "Base.astro emits no Google Fonts <link> and no fonts.googleapis.com / fonts.gstatic.com preconnect"
    - "Fredoka and Poppins load from public/fonts/ via @font-face in global.css"
    - "On the production (Cloudflare, base /) build, Fredoka 600/700 and Poppins 400/500/600/700 resolve without faux-bold synthesis"
    - "Critical font weights are preloaded with base-prefixed hrefs that do not 404 on GitHub Pages"
    - "Both npm run build and GITHUB_PAGES=true npm run build stay green"
  artifacts:
    - path: "public/fonts/Fredoka-SemiBold.woff2"
      provides: "Self-hosted Fredoka 600"
    - path: "public/fonts/Poppins-Regular.woff2"
      provides: "Self-hosted Poppins 400"
    - path: "src/styles/global.css"
      provides: "@font-face declarations for Fredoka + Poppins matching tailwind.config family names"
      contains: "@font-face"
    - path: "src/layouts/Base.astro"
      provides: "Google Fonts link removed; base-prefixed font preloads added"
  key_links:
    - from: "src/styles/global.css"
      to: "public/fonts/*.woff2"
      via: "@font-face src url('/fonts/...')"
      pattern: "@font-face"
    - from: "src/layouts/Base.astro"
      to: "public/fonts/*.woff2"
      via: "rel=preload base-prefixed href"
      pattern: "rel=[\"']preload[\"']"
---

<objective>
Self-host Fredoka and Poppins from `public/fonts/` via `@font-face`, remove the render-blocking Google Fonts `<link>` and preconnects from Base.astro, and preload the critical weights. This removes a third-party dependency (GDPR + performance win) and eliminates render-blocking external requests.

Purpose: PERF-05 — self-hosted fonts, Google Fonts `<link>` removed. Visual parity required: same families/weights (Fredoka 600/700, Poppins 400/500/600/700) currently loaded from Google Fonts. Per RESEARCH.md, Poppins-Bold (700) is missing locally — mitigate by declaring Poppins-SemiBold at weight 700 to avoid faux-bold synthesis.

Output: 5 woff2 files copied into `public/fonts/`, `@font-face` block in `src/styles/global.css`, modified `src/layouts/Base.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-PATTERNS.md

<interfaces>
<!-- Font assets, family names, and base-path facts the executor must respect. -->

Source font files in design-system-assets/fonts/ (copy the 5 woff2, NOT README.md):
- Fredoka-SemiBold.woff2  -> family 'Fredoka', weight 600
- Fredoka-Bold.woff2      -> family 'Fredoka', weight 700
- Poppins-Regular.woff2   -> family 'Poppins', weight 400
- Poppins-Medium.woff2    -> family 'Poppins', weight 500
- Poppins-SemiBold.woff2  -> family 'Poppins', weight 600 AND 700 (gap mitigation: Poppins-Bold is missing; declare SemiBold at 700 too so Tailwind `font-bold` does not trigger faux-bold).

Family names MUST match tailwind.config.mjs:65-68 exactly:
  fontFamily: { sans: ['Poppins', ...], display: ['Fredoka', 'Poppins', ...] }
-> @font-face family strings are exactly 'Fredoka' and 'Poppins'.

Google Fonts block to REMOVE from src/layouts/Base.astro (currently ~lines 50-54):
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Fredoka:wght@600;700&family=Poppins:wght@400;500;600;700&display=swap" />

Base-prefix pattern in Base.astro (line 5, and used at lines 46-47 for icon/apple-touch hrefs):
  const base = import.meta.env.BASE_URL.replace(/\/$/, '');
  href={`${base}/favicon.svg`}
-> font preload hrefs MUST use `${base}/fonts/...` or they 404 on GitHub Pages.

global.css structure (lines 1-15): Tailwind directives, then `@layer base { :root { ... } }`. No @font-face exists yet. Convention: `/* ===== SECTION NAME ===== */` banner comments.

@font-face url() CONSTRAINT (locked tradeoff): plain CSS `url()` cannot interpolate import.meta.env.BASE_URL. Use root-relative `url('/fonts/...')`. This resolves correctly on production (Cloudflare, base `/`) where visual parity is REQUIRED. On GitHub Pages (base `/evolea-website`) the `/fonts/...` path will 404 and the fallback system fonts apply — this is ACCEPTABLE because GitHub Pages is the fallback-only site and the hard requirement is (a) both builds green and (b) visual parity on production only. Document this tradeoff in a CSS comment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Copy fonts and declare @font-face in global.css</name>
  <read_first>
    - design-system-assets/fonts/ (the 5 source woff2 files)
    - src/styles/global.css (lines 1-15 — Tailwind directives + @layer base structure; banner-comment convention)
    - tailwind.config.mjs (around lines 65-68 — fontFamily names that @font-face must match exactly)
  </read_first>
  <files>public/fonts/Fredoka-SemiBold.woff2, public/fonts/Fredoka-Bold.woff2, public/fonts/Poppins-Regular.woff2, public/fonts/Poppins-Medium.woff2, public/fonts/Poppins-SemiBold.woff2, src/styles/global.css</files>
  <action>
    Copy exactly these 5 files from design-system-assets/fonts/ into public/fonts/ (create the dir): Fredoka-SemiBold.woff2, Fredoka-Bold.woff2, Poppins-Regular.woff2, Poppins-Medium.woff2, Poppins-SemiBold.woff2. Do NOT copy README.md.

    Add a `/* ===== SELF-HOSTED FONTS (Fredoka / Poppins) ===== */` banner section near the top of global.css (after the Tailwind directives, inside or just above `@layer base` per Tailwind 3 convention) with six `@font-face` blocks. Each uses `font-style: normal; font-display: swap;` and root-relative `src: url('/fonts/<file>.woff2') format('woff2');`. The six declarations:
    - family 'Fredoka', weight 600 -> Fredoka-SemiBold.woff2
    - family 'Fredoka', weight 700 -> Fredoka-Bold.woff2
    - family 'Poppins', weight 400 -> Poppins-Regular.woff2
    - family 'Poppins', weight 500 -> Poppins-Medium.woff2
    - family 'Poppins', weight 600 -> Poppins-SemiBold.woff2
    - family 'Poppins', weight 700 -> Poppins-SemiBold.woff2 (GAP MITIGATION: Poppins-Bold is missing; mapping SemiBold to 700 prevents faux-bold synthesis on Tailwind `font-bold` elements)

    Add a comment in the section noting the source is design-system-assets/fonts/, the Poppins-700->SemiBold gap mitigation, and the GitHub-Pages base-path tradeoff (root-relative `/fonts/` resolves on production Cloudflare base `/`; GitHub Pages fallback falls back to system fonts — acceptable per locked decision).
  </action>
  <verify>
    <automated>ls public/fonts/*.woff2 | wc -l; grep -c "@font-face" src/styles/global.css; grep -c "font-display: swap" src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/fonts/*.woff2 | wc -l` returns `5`.
    - `ls public/fonts/README.md` fails (README not copied).
    - `grep -c "@font-face" src/styles/global.css` returns `6`.
    - `grep -c "font-family: 'Poppins'" src/styles/global.css` returns `4`.
    - Poppins-SemiBold.woff2 referenced for BOTH 600 and 700: `grep -c "url('/fonts/Poppins-SemiBold.woff2')" src/styles/global.css` returns `2`.
    - `grep -c "font-display: swap" src/styles/global.css` returns `6`.
    - All six src use root-relative urls: `grep -c "url('/fonts/" src/styles/global.css` returns `6`.
  </acceptance_criteria>
  <done>5 woff2 files in public/fonts/; six @font-face declarations in global.css with correct family/weight mapping including Poppins 700->SemiBold gap mitigation; all use font-display: swap and root-relative urls.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Google Fonts link from Base.astro and add base-prefixed preloads</name>
  <read_first>
    - src/layouts/Base.astro (lines 5 and 46-54 — the base-prefix const, the icon hrefs to copy the pattern from, and the Google Fonts preconnect+link block to remove)
  </read_first>
  <files>src/layouts/Base.astro</files>
  <action>
    In src/layouts/Base.astro `<head>`, DELETE the three Google Fonts lines: the two `<link rel="preconnect">` to fonts.googleapis.com and fonts.gstatic.com, and the `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?...">`.

    In their place add `<link rel="preload">` tags for the two critical above-the-fold weights ONLY (preloading all five wastes bandwidth): Fredoka-SemiBold.woff2 (display headlines) and Poppins-Regular.woff2 (body). Use the existing base-prefix pattern exactly as the icon hrefs do: `href={`${base}/fonts/Fredoka-SemiBold.woff2`}` and `href={`${base}/fonts/Poppins-Regular.woff2`}`, each with `rel="preload" as="font" type="font/woff2" crossorigin`. The `base` const already exists at line 5; do not redeclare it.
  </action>
  <verify>
    <automated>grep -c "fonts.googleapis.com\|fonts.gstatic.com" src/layouts/Base.astro; grep -c 'rel="preload"' src/layouts/Base.astro; npm run build 2>&1 | tail -8; echo "---GH---"; GITHUB_PAGES=true npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fonts.googleapis.com\|fonts.gstatic.com" src/layouts/Base.astro` returns `0` (all Google Fonts references removed).
    - At least two `rel="preload"` font links exist with `as="font"` and `type="font/woff2"` and `crossorigin`: `grep -c 'as="font"' src/layouts/Base.astro` returns `2`.
    - Both preload hrefs use the `${base}/fonts/` interpolation: `grep -c 'base}/fonts/' src/layouts/Base.astro` returns `2`.
    - `npm run build` exits 0.
    - `GITHUB_PAGES=true npm run build` exits 0.
  </acceptance_criteria>
  <done>Google Fonts link + both preconnects removed from Base.astro; two base-prefixed woff2 preloads added; both builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> third-party font CDN | Removing Google Fonts eliminates a third-party request boundary (GDPR/privacy improvement) |
| build -> dist asset paths | Root-relative font urls must resolve on the production base path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information disclosure | Google Fonts CDN leak (IP/UA to Google) | mitigate | Self-hosting fonts removes the third-party request entirely; no client data sent to fonts.gstatic.com. |
| T-01-07 | Denial of availability | Font 404 on GitHub Pages base path | accept | Root-relative `/fonts/` 404s on GitHub Pages fallback and degrades to system fonts; production (Cloudflare base `/`) resolves correctly. Documented locked tradeoff; both builds stay green. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (font files copied from in-repo design-system-assets/). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run build` and `GITHUB_PAGES=true npm run build` both exit 0.
- Greps confirm Google Fonts removed, six @font-face present, preloads base-prefixed.
- DEFERRED to phase verification (browser-only): visual parity check that headlines render in Fredoka and body in Poppins with no faux-bold on `font-bold` Poppins elements on the production deploy; Network panel shows zero requests to fonts.gstatic.com.
</verification>

<success_criteria>
- PERF-05: Fredoka/Poppins self-hosted from public/fonts/ via @font-face; Google Fonts `<link>` (and preconnects) removed from Base.astro; critical weights preloaded with base-prefixed hrefs; both builds green; Poppins-700 gap mitigated to avoid faux-bold.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-security/01-02-SUMMARY.md` when done.
</output>
