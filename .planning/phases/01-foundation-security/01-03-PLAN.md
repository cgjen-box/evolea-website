---
phase: 01-foundation-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/programs/tagesschule-hero.png
  - src/pages/angebote/tagesschule/index.astro
  - src/pages/en/programs/day-school/index.astro
  - public/images/generated/
  - public/images/Final images/
  - public/images/logo/
  - src/components/AngeboteSection.astro
  - src/components/TimelineActivities.astro
  - src/components/ProgramCardEnhanced.astro
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
must_haves:
  truths:
    - "git ls-files public/images/generated/ returns nothing (82MB blob untracked from HEAD)"
    - "The tagesschule hero resolves from public/images/programs/ in both the DE and EN page wrappers with no broken-image reference to /images/generated/"
    - "public/images/Final images/ and the four superseded logo originals are deleted"
    - "The three dead components are deleted (grep-confirmed zero references)"
    - "Both npm run build and GITHUB_PAGES=true npm run build stay green"
  artifacts:
    - path: "public/images/programs/tagesschule-hero.png"
      provides: "Relocated tagesschule hero in a tracked location"
    - path: "src/pages/angebote/tagesschule/index.astro"
      provides: "DE wrapper with hero fallback pointing at /images/programs/"
      contains: "/images/programs/tagesschule-hero.png"
    - path: "src/pages/en/programs/day-school/index.astro"
      provides: "EN wrapper with hero fallback pointing at /images/programs/"
      contains: "/images/programs/tagesschule-hero.png"
  key_links:
    - from: "src/pages/angebote/tagesschule/index.astro"
      to: "public/images/programs/tagesschule-hero.png"
      via: "heroImage fallback base + path"
      pattern: "/images/programs/tagesschule-hero.png"
    - from: "src/pages/en/programs/day-school/index.astro"
      to: "public/images/programs/tagesschule-hero.png"
      via: "heroImage fallback base + path"
      pattern: "/images/programs/tagesschule-hero.png"
---

<objective>
Clean the repo: relocate the tagesschule hero out of the gitignored-but-committed 82MB `public/images/generated/` directory into the tracked `public/images/programs/`, update both DE and EN references, untrack `generated/` from HEAD, delete the unreferenced `Final images/` (7.2MB) and four superseded logo originals, and delete three dead components after grep confirmation.

Purpose: HYG-01 (relocate hero first), HYG-02 (untrack generated/, delete Final images + logo originals — each gated on grep), HYG-03 (delete dead components after grep). The order of operations is LOCKED in CONTEXT.md: move hero -> update references -> untrack generated/ -> delete the rest. History rewrite is explicitly NOT in scope (untracking removes from HEAD only).

Output: relocated hero, two updated page wrappers, generated/ untracked, deletions committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-PATTERNS.md

<interfaces>
<!-- Verified-against-codebase facts the executor must respect (2026-06-12). -->

tagesschule hero reference (IDENTICAL in both files, around lines 18-20):
  src/pages/angebote/tagesschule/index.astro
  src/pages/en/programs/day-school/index.astro
  const heroImage = siteImages?.programmeHeroes?.tagesschule
    ? base + siteImages.programmeHeroes.tagesschule
    : base + "/images/generated/tagesschule-hero.png";
-> change ONLY the fallback string to `base + "/images/programs/tagesschule-hero.png"`.

CMS override is NOT in play: src/content/settings/images.json programmeHeroes.tagesschule is `null` (verified) — so the fallback string is what actually renders. Do NOT need to edit images.json. (There is also a `videoSources.tagesschule` pointing at homepage-hero.png — unrelated, leave it.)

Source file to MOVE: public/images/generated/tagesschule-hero.png (exists, verified). There is also a timestamped variant tagesschule-hero_20251205_131016.png in generated/ — that one is NOT referenced, it goes away with the untracking.

generated/ is already gitignored (.gitignore:42 `public/images/generated/`) but 26 files are tracked from before the ignore was added. `git rm -r --cached public/images/generated/` removes them from HEAD; the files stay on disk (still ignored). Do this AFTER the hero is copied to public/images/programs/ and both references updated.

Deletion targets — grep-verified safe (01-PATTERNS.md deletion table):
- Dead components (zero imports in src/): src/components/AngeboteSection.astro, src/components/TimelineActivities.astro, src/components/ProgramCardEnhanced.astro
- public/images/Final images/ (7.2MB, 8 tracked files, zero references in src/ or content JSONs)
- Four superseded logo originals in public/images/logo/: `Evolea Logo.png`, `Evolea New Logo.png`, `evolea-logo-original.png`, `evolea-logo.png`
- KEEP (in use — do NOT delete): public/images/logo/evolea-logo-new.png (3 refs), evolea-logo-circle.png (1 ref), evolea-butterfly.svg, evolea-butterfly-white.svg

Dual-build gate: both `npm run build` and `GITHUB_PAGES=true npm run build` must stay green after the changes.

Pre-commit: gitleaks + scripts/check_secrets.py + `npm run build` run on commit; no `--no-verify`. Use the cgjen-box GitHub account.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Relocate the tagesschule hero and update DE + EN references</name>
  <read_first>
    - src/pages/angebote/tagesschule/index.astro (lines 18-20 — the heroImage fallback to change)
    - src/pages/en/programs/day-school/index.astro (lines 18-20 — identical fallback to change)
    - public/images/generated/ (confirm tagesschule-hero.png is present before moving)
  </read_first>
  <files>public/images/programs/tagesschule-hero.png, src/pages/angebote/tagesschule/index.astro, src/pages/en/programs/day-school/index.astro</files>
  <action>
    Copy public/images/generated/tagesschule-hero.png to public/images/programs/tagesschule-hero.png (the programs/ dir already exists and is tracked). Use a copy (not a git mv from an ignored path) so the new file is a fresh tracked addition.

    In BOTH src/pages/angebote/tagesschule/index.astro and src/pages/en/programs/day-school/index.astro, change ONLY the heroImage fallback string from `base + "/images/generated/tagesschule-hero.png"` to `base + "/images/programs/tagesschule-hero.png"`. Leave the CMS-override branch (`siteImages?.programmeHeroes?.tagesschule`) untouched. Do not edit src/content/settings/images.json (the tagesschule override is null and unrelated videoSources entry stays).

    `git add` the new public/images/programs/tagesschule-hero.png so it is tracked.
  </action>
  <verify>
    <automated>test -f public/images/programs/tagesschule-hero.png && echo "hero present"; grep -c "/images/generated/tagesschule-hero.png" src/pages/angebote/tagesschule/index.astro src/pages/en/programs/day-school/index.astro; grep -c "/images/programs/tagesschule-hero.png" src/pages/angebote/tagesschule/index.astro src/pages/en/programs/day-school/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `public/images/programs/tagesschule-hero.png` exists.
    - `git ls-files public/images/programs/tagesschule-hero.png` returns the path (tracked).
    - Neither page wrapper references `/images/generated/tagesschule-hero.png` any more: `grep -rc "/images/generated/tagesschule-hero.png" src/` returns `0`.
    - BOTH wrappers reference `/images/programs/tagesschule-hero.png`: the grep across both files returns 1 match each.
    - src/content/settings/images.json is unchanged (no diff).
  </acceptance_criteria>
  <done>Hero copied to tracked public/images/programs/ and git-added; both DE and EN fallbacks point at /images/programs/; no remaining reference to /images/generated/ in src/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Untrack generated/, delete Final images, logo originals, and dead components</name>
  <read_first>
    - .gitignore (line 42 — confirm public/images/generated/ already ignored)
    - .planning/phases/01-foundation-security/01-PATTERNS.md (the Deletion Verification grep-evidence table)
  </read_first>
  <files>public/images/generated/, public/images/Final images/, public/images/logo/, src/components/AngeboteSection.astro, src/components/TimelineActivities.astro, src/components/ProgramCardEnhanced.astro</files>
  <action>
    Only after Task 1 confirms the hero is relocated and referenced:

    1. Re-confirm safety with grep before each deletion (defense in depth, the PATTERNS table is the prior evidence):
       - `grep -rn "AngeboteSection\b" src/ | grep -v "AngeboteSectionV3"` should be empty (the V3 component is the live one — do not confuse).
       - `grep -rn "TimelineActivities" src/` should be empty.
       - `grep -rn "ProgramCardEnhanced" src/` should be empty.
       - `grep -rn "Final images" src/ public/` (excluding the dir itself) should be empty.
       - For each logo original, grep its basename across src/ and src/content — must be zero.
       If ANY grep returns a live reference, STOP and report rather than delete.

    2. `git rm -r --cached public/images/generated/` — removes the 26 tracked files from HEAD; files remain on disk (still gitignored). Do NOT `rm` the disk files.

    3. `git rm -r "public/images/Final images/"` — delete the 8 tracked files (disk + index).

    4. `git rm` the four superseded logo originals: `public/images/logo/Evolea Logo.png`, `public/images/logo/Evolea New Logo.png`, `public/images/logo/evolea-logo-original.png`, `public/images/logo/evolea-logo.png`. KEEP evolea-logo-new.png, evolea-logo-circle.png, and both butterfly SVGs.

    5. `git rm` the three dead components: src/components/AngeboteSection.astro, src/components/TimelineActivities.astro, src/components/ProgramCardEnhanced.astro.
  </action>
  <verify>
    <automated>git ls-files public/images/generated/ | wc -l; git ls-files "public/images/Final images/" | wc -l; ls src/components/AngeboteSection.astro src/components/TimelineActivities.astro src/components/ProgramCardEnhanced.astro 2>&1 | grep -c "No such file"; git ls-files public/images/logo/ | grep -ic "evolea-logo-new.png\|evolea-logo-circle.png"; npm run build 2>&1 | tail -8; echo "---GH---"; GITHUB_PAGES=true npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files public/images/generated/ | wc -l` returns `0` (untracked from HEAD).
    - `public/images/generated/tagesschule-hero.png` still exists ON DISK (`test -f` succeeds) — untracking did not delete the working copy.
    - `git ls-files "public/images/Final images/" | wc -l` returns `0`.
    - The three dead components no longer exist: `ls src/components/AngeboteSection.astro` etc. all fail.
    - The four superseded logo originals are gone from tracking, and the KEEP files remain: `git ls-files public/images/logo/` includes evolea-logo-new.png, evolea-logo-circle.png, both butterfly SVGs, and excludes the four originals.
    - `grep -rn "AngeboteSection\b" src/ | grep -v AngeboteSectionV3` is empty; `grep -rn "TimelineActivities\|ProgramCardEnhanced" src/` is empty.
    - `npm run build` exits 0.
    - `GITHUB_PAGES=true npm run build` exits 0 (no broken-image build errors; tagesschule hero resolves).
  </acceptance_criteria>
  <done>generated/ untracked from HEAD (disk copy intact), Final images/ and four logo originals deleted, three dead components deleted (all grep-reconfirmed), keep-list logos intact, both builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index -> deployed dist | Deleting/untracking files must not remove an asset a live page references |
| working tree -> commit | check_secrets.py + gitleaks pre-commit hooks gate the commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Denial of availability | Deleting an in-use asset/component breaks a live page | mitigate | Each deletion is grep-reconfirmed at execution time (not just from the prior PATTERNS table); the hero is relocated and re-referenced BEFORE generated/ is untracked; both builds must stay green as the final gate. |
| T-01-09 | Tampering | git rm --cached vs rm confusion deletes the working hero | mitigate | Explicit instruction: `--cached` only for generated/, never `rm` the disk file; acceptance criterion asserts the on-disk hero survives. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run build` and `GITHUB_PAGES=true npm run build` both exit 0 (broken-image / dead-import regressions caught here).
- `git ls-files` assertions confirm generated/ and Final images/ untracked, dead components gone, keep-list logos retained.
- DEFERRED to phase verification (browser-only): tagesschule DE and EN pages render the hero from /images/programs/ with no broken-image reference, on the production deploy.
</verification>

<success_criteria>
- HYG-01: tagesschule-hero.png moved to public/images/programs/ with both DE+EN fallbacks updated, BEFORE generated-image cleanup.
- HYG-02: public/images/generated/ untracked from git; Final images/ and four superseded logo originals deleted (each grep-gated).
- HYG-03: three dead components deleted after grep confirmation.
- Both builds green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-security/01-03-SUMMARY.md` when done.
</output>
