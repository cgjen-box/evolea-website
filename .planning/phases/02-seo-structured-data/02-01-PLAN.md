---
phase: 02-seo-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo.ts
  - src/middleware.ts
  - src/pages/robots.txt.ts
  - astro.config.mjs
  - src/layouts/Base.astro
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]
tags: [seo, canonical, robots, sitemap, middleware, redirects]

must_haves:
  truths:
    - "A GET to an SSR page URL without a trailing slash (e.g. /angebote) 301-redirects to the trailing-slash form, preserving the query string, with no redirect loop"
    - "A pathname starting with // is collapsed to a single leading slash before redirecting (no protocol-relative open redirect)"
    - "/api/* paths, /keystatic paths, /_-prefixed paths, and paths with file extensions are never redirected by the new middleware"
    - "robots.txt on hostname www.evolea.ch serves Allow: / plus a Sitemap: line and Disallow: /keystatic/; on any other hostname it serves Disallow: /"
    - "/sitemap.xml 301-redirects to /sitemap-index.xml on the Cloudflare build; the static build emits a redirect stub"
    - "Every page head: canonical ends with a trailing slash, og:url and twitter:url equal the canonical (not raw Astro.url), <link rel=sitemap> is present, and the title suffix is '– EVOLEA' (en dash) in all four emission points"
    - "The static (GITHUB_PAGES) build emits <meta name=robots content=noindex> on every page; the Cloudflare SSR build does not"
    - "No /brand/ URL appears in the generated sitemap"
  artifacts:
    - path: "src/pages/robots.txt.ts"
      provides: "Hostname-keyed robots.txt SSR endpoint (prerendered deny variant on static builds)"
      exports: ["GET", "prerender"]
    - path: "src/lib/seo.ts"
      provides: "PROD_HOST constant (single source for hostname keying)"
      exports: ["PROD_HOST"]
    - path: "src/middleware.ts"
      provides: "trailingSlash middleware first in sequence(); host-keyed X-Robots-Tag on SSR responses"
      contains: "sequence(trailingSlash, securityHeaders, keystaticEnhancements)"
    - path: "astro.config.mjs"
      provides: "/sitemap.xml 301 redirect + sitemap filter excluding /brand/"
      contains: "'/sitemap.xml'"
    - path: "src/layouts/Base.astro"
      provides: "Slash-normalized canonicalURL, og:url/twitter:url from canonical, ogType prop, rel=sitemap link, '– EVOLEA' title separator, static-build noindex meta"
      contains: "rel=\"sitemap\""
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/seo.ts"
      via: "PROD_HOST named import"
      pattern: "from '@/lib/seo'"
    - from: "src/pages/robots.txt.ts"
      to: "sitemap-index.xml"
      via: "Sitemap: line built from the site param"
      pattern: "sitemap-index\\.xml"
    - from: "src/layouts/Base.astro"
      to: "canonicalURL"
      via: "og:url and twitter:url both render canonicalURL"
      pattern: "og:url\" content=\\{canonicalURL\\}"
---

<objective>
Enforce one canonical URL form (trailing slash) via a first-in-sequence middleware 301, make the site discoverable (hostname-keyed robots.txt, /sitemap.xml redirect, rel=sitemap link), and fix the head metadata that currently leaks duplicate URL forms (og:url/twitter:url from raw Astro.url, slashless canonical) — closing SEO-01, SEO-02, SEO-03.

Purpose: Live probes proved https://www.evolea.ch/angebote returns 200 with a slashless canonical (duplicate content), robots.txt and /sitemap.xml both 404, and *.pages.dev serves indexable content. This plan fixes all of it at the middleware/config/layout level.
Output: New `src/pages/robots.txt.ts` + `src/lib/seo.ts` (PROD_HOST), modified `src/middleware.ts`, `astro.config.mjs`, `src/layouts/Base.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-seo-structured-data/02-RESEARCH.md
@.planning/phases/02-seo-structured-data/02-PATTERNS.md
@.planning/phases/01-foundation-security/01-01-SUMMARY.md
@src/middleware.ts
@src/layouts/Base.astro
@astro.config.mjs
@src/pages/api/csp-report.ts

Locked decisions honored by this plan:
- D1: trailing-slash enforcement via first-in-`sequence()` middleware 301 (NOT `trailingSlash: 'always'`), with `/api/*` + `/keystatic` exemptions and the `//` open-redirect collapse.
- D2: robots.txt as an SSR endpoint reusing the `__SSR_BUILD__` prerender pattern; production hostname allows, every other hostname denies; static build prerenders the deny variant and Base adds a noindex meta.
- D5: filter `/brand/` pages out of the sitemap.
- Discretion (RESEARCH Pitfall 3): SSR responses on non-production hostnames also get `X-Robots-Tag: noindex` from the middleware. `public/_headers` is deliberately NOT touched (avoids the gen-headers.mjs parity-gate churn of Pitfall 5; static assets on *.pages.dev remain covered by the deny robots.txt).
</context>

<interfaces>
Created by this plan, consumed by later plans:

From src/lib/seo.ts (this plan creates the module; plan 02-02 extends it):
```typescript
export const PROD_HOST = 'www.evolea.ch';
```

From src/layouts/Base.astro (new prop consumed by blog templates in plan 02-05):
```typescript
interface Props {
  title: string;
  description?: string;
  image?: string;
  transparentHeader?: boolean;
  hideFooterCTA?: boolean;
  ogType?: 'website' | 'article'; // NEW — default 'website'
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add trailingSlash middleware first in sequence() and host-keyed X-Robots-Tag</name>
  <files>src/lib/seo.ts, src/middleware.ts</files>
  <read_first>src/middleware.ts (entire file — the securityHeaders member is the structural analog), src/lib/security-headers.ts (banner-doc style for the new seo.ts module), .planning/phases/02-seo-structured-data/02-RESEARCH.md §Pattern 1 (the complete trailingSlash implementation including the open-redirect guard)</read_first>
  <action>
    Create `src/lib/seo.ts` with the same banner-doc style as `src/lib/security-headers.ts` (single-source-of-truth comment listing consumers: src/middleware.ts and src/pages/robots.txt.ts today; plan 02-02 will extend with schema builders). Export `export const PROD_HOST = 'www.evolea.ch';` — nothing else yet.

    In `src/middleware.ts`, add a new `trailingSlash` member ABOVE `securityHeaders`, copying RESEARCH Pattern 1 verbatim (per D1): `defineMiddleware((context, next) => ...)` that 301-redirects when ALL of: method is GET or HEAD; `pathname` does not end with `/`; pathname has no file extension (regex `/\.[^/]+$/`); pathname is not exempt. Exemptions (exact-or-slash style matching the existing `/keystatic` pattern in this file): `pathname.startsWith('/api/')`, `pathname === '/keystatic' || pathname.startsWith('/keystatic/')`, `pathname.startsWith('/_')`. Before redirecting, collapse duplicate leading slashes with `pathname.replace(/^\/+/, '/')` so `//evil.com` can never become a protocol-relative `Location` (open-redirect mitigation, per D1/D7). Redirect target is the collapsed path + `/` + `context.url.search`, status 301. Non-matching requests fall through with `return next()`. Note this member SHORT-CIRCUITS (returns a redirect instead of calling next) — unlike the existing post-processing members.

    Change the export to `sequence(trailingSlash, securityHeaders, keystaticEnhancements)` and update the ordering comment: trailingSlash runs first and short-circuits 301s before securityHeaders, so redirect responses carry no security headers — acceptable, 301s have no body.

    In the existing `securityHeaders` member's try block, after the CSP line, add: `import { PROD_HOST } from '@/lib/seo';` at the top of the file and set `response.headers.set('X-Robots-Tag', 'noindex')` when `context.url.hostname !== PROD_HOST`. This de-indexes *.pages.dev SSR responses (defense-in-depth, RESEARCH Pitfall 3). Do NOT touch public/_headers.
  </action>
  <verify>
    <automated>node scripts/gen-headers.mjs && grep -q "sequence(trailingSlash, securityHeaders, keystaticEnhancements)" src/middleware.ts && grep -qF "replace(/^\/+/, '/')" src/middleware.ts && grep -q "X-Robots-Tag" src/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/seo.ts` exists and `grep -c "export const PROD_HOST = 'www.evolea.ch'" src/lib/seo.ts` returns 1
    - `grep -c "sequence(trailingSlash, securityHeaders, keystaticEnhancements)" src/middleware.ts` returns 1
    - The trailingSlash member contains the leading-slash collapse (`replace(/^\/+/, '/')` or equivalent) applied BEFORE building the redirect Location
    - `grep -c "startsWith('/api/')" src/middleware.ts` >= 1 and the `/keystatic` exact-or-slash exemption and `/_` prefix exemption are present in the trailingSlash member
    - Redirect status is 301 and the search string is appended to the target
    - `grep -c "X-Robots-Tag" src/middleware.ts` returns 1, conditioned on `hostname !== PROD_HOST`
    - `node scripts/gen-headers.mjs` exits 0 (public/_headers untouched)
    - Each middleware member still calls `next()` at most once; no `next()` call appears in any catch block
  </acceptance_criteria>
  <done>Middleware enforces trailing-slash 301s with exemptions and open-redirect guard; non-production SSR responses carry X-Robots-Tag: noindex; parity gate stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Hostname-keyed robots.txt endpoint + /sitemap.xml redirect + /brand/ sitemap filter</name>
  <files>src/pages/robots.txt.ts, astro.config.mjs</files>
  <read_first>src/pages/api/csp-report.ts (the proven __SSR_BUILD__ prerender pattern and its explanatory comment block — copy that header structure), astro.config.mjs (existing redirects entry for the cafe page shows the base-path-prefix shape to copy; vite.define already provides __SSR_BUILD__), .planning/phases/02-seo-structured-data/02-RESEARCH.md §Pattern 2</read_first>
  <action>
    Create `src/pages/robots.txt.ts` per RESEARCH Pattern 2 (D2): `declare const __SSR_BUILD__: boolean;` + `export const prerender = !__SSR_BUILD__;` with the same explanatory comment style as csp-report.ts. Export `GET: APIRoute` destructuring `{ url, site }`. Import `PROD_HOST` from `@/lib/seo`. `isProd = __SSR_BUILD__ && url.hostname === PROD_HOST` (default-deny: only the exact production host is crawlable). Production body lines: `User-agent: *`, `Allow: /`, `Disallow: /keystatic/`, `Disallow: /api/`, blank line, `Sitemap: ${new URL('sitemap-index.xml', site)}`. Every other host (and every static build): `User-agent: *\nDisallow: /\n`. Response header `Content-Type: text/plain; charset=utf-8`.

    In `astro.config.mjs` redirects, add `'/sitemap.xml': { status: 301, destination: ... }` where destination is the base-prefixed `/sitemap-index.xml` using the exact same `${isGitHubPages || !useCloudflare ? '/evolea-website' : ''}` expression as the existing cafe redirect (Astro does not prefix redirect targets). On the Cloudflare adapter this is a real 301; on static builds it emits a meta-refresh stub (acceptable for the de-indexed mirror).

    Change `sitemap()` to `sitemap({ filter: (page) => !page.includes('/brand/') })` (D5 — /brand/ pages are internal style-guide pages currently leaking into the production sitemap).
  </action>
  <verify>
    <automated>npm run build && grep -q "Disallow: /" dist/robots.txt && test -f dist/sitemap.xml && ! grep -q "/brand/" dist/sitemap-0.xml</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (static/adapter-absent local build)
    - `dist/robots.txt` exists and contains exactly the deny variant (`Disallow: /`, no `Allow:` line, no `Sitemap:` line) — the prerendered GET ran with __SSR_BUILD__=false
    - `src/pages/robots.txt.ts` source: the production branch is gated on BOTH `__SSR_BUILD__` AND `url.hostname === PROD_HOST` (imported from '@/lib/seo', not a duplicated literal), and the production body contains `Allow: /`, `Disallow: /keystatic/`, `Disallow: /api/`, and a `Sitemap:` line built from `new URL('sitemap-index.xml', site)`
    - `dist/sitemap.xml` exists (static redirect stub) and `astro.config.mjs` contains a `'/sitemap.xml'` redirect entry with status 301 and a base-path-aware destination
    - `grep -c "/brand/" dist/sitemap-0.xml` returns 0; sitemap-index.xml and sitemap-0.xml still generated
  </acceptance_criteria>
  <done>robots.txt is hostname-keyed default-deny with the proven dual-build pattern; /sitemap.xml 301s to the real index; /brand/ no longer leaks into the sitemap.</done>
</task>

<task type="auto">
  <name>Task 3: Base.astro head fixes — canonical normalization, og/twitter URL, ogType, rel=sitemap, title separator, static noindex</name>
  <files>src/layouts/Base.astro</files>
  <read_first>src/layouts/Base.astro (entire file — all edits are surgical; the four title emission points are at the <title>, meta name=title, og:title, twitter:title lines; canonical computation at line ~32; og:url/twitter:url at lines ~63/71), .planning/phases/02-seo-structured-data/02-RESEARCH.md §Pattern 5 and Pitfall 4, src/pages/api/csp-report.ts (for the __SSR_BUILD__ declare pattern)</read_first>
  <action>
    Six surgical edits to `src/layouts/Base.astro` (SEO-02/SEO-03 head halves + SEO-09 separator mechanics):

    1. Canonical normalization (RESEARCH Pattern 5): replace `const canonicalURL = new URL(Astro.url.pathname, Astro.site)` with a slash-normalized version — compute `canonicalPath` as `Astro.url.pathname` if it already ends with `/`, else `pathname + '/'`, then `new URL(canonicalPath, Astro.site)`.
    2. og:url and twitter:url: change both `content={Astro.url}` to `content={canonicalURL}`.
    3. ogType prop: extend `interface Props` with `ogType?: 'website' | 'article'`, default `'website'` in the destructure; change `<meta property="og:type" content="website" />` to `content={ogType}`.
    4. Title separator: change all FOUR emission points (`<title>`, `meta name="title"`, `og:title`, `twitter:title`) from `{title} | EVOLEA` to `{title} – EVOLEA` (en dash U+2013). No `| EVOLEA` may remain anywhere in the file.
    5. Sitemap discovery link: next to the canonical block add `<link rel="sitemap" type="application/xml" href={`${base}/sitemap-index.xml`} />` using the existing `${base}` favicon prefix pattern.
    6. Static-build noindex (D2 + RESEARCH Pitfall 4): add `declare const __SSR_BUILD__: boolean;` in the frontmatter (same proven Vite-define pattern as csp-report.ts) and emit `<meta name="robots" content="noindex, nofollow" />` in the head only when `!__SSR_BUILD__`. This neutralizes the GitHub Pages mirror (its robots.txt sits at a subpath crawlers never read) and the local adapter-absent static build (never deployed to production — Cloudflare builds always have the adapter, CF_PAGES=1 throws otherwise).

    Do NOT touch the hreflang/alternates block (existing correct behavior to preserve) and do NOT add JSON-LD here (plan 02-02).
  </action>
  <verify>
    <automated>npm run build && grep -q 'rel="sitemap"' dist/index.html && grep -q 'name="robots" content="noindex' dist/index.html && ! grep -q '| EVOLEA' src/layouts/Base.astro && test "$(grep -c '– EVOLEA' src/layouts/Base.astro)" = "4"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 AND `GITHUB_PAGES=true npm run build` exits 0
    - In dist (static build): `grep -o 'rel="canonical" href="[^"]*"' dist/angebote/index.html` shows an href ending in `/angebote/` (trailing slash); og:url and twitter:url in the same file carry the identical href
    - `grep -c '– EVOLEA' src/layouts/Base.astro` returns 4 and `grep -c '| EVOLEA' src/layouts/Base.astro` returns 0
    - `grep -c 'ogType' src/layouts/Base.astro` >= 3 (interface, destructure default 'website', og:type emission)
    - `dist/index.html` contains `rel="sitemap"` with an href ending in `/sitemap-index.xml`
    - `dist/index.html` contains `<meta name="robots" content="noindex, nofollow"` and the emission is source-gated on `!__SSR_BUILD__` (so Cloudflare SSR pages will NOT carry it)
    - hreflang alternate links unchanged (same count as before the edit)
  </acceptance_criteria>
  <done>Every page head emits one slash-normalized canonical mirrored by og:url/twitter:url, advertises the sitemap, supports og:type switching, suffixes titles with '– EVOLEA', and noindexes static-mirror builds.</done>
</task>

</tasks>

<threat_model>
Per phase decision D7, lightweight register (no full STRIDE block required):

| Threat | Component | Disposition | Mitigation |
|--------|-----------|-------------|------------|
| Open redirect via `//host` pathname (protocol-relative Location) | trailingSlash middleware | mitigate | Collapse leading slashes (`replace(/^\/+/, '/')`) before building Location — asserted in Task 1 acceptance criteria |
| Host-header spoofing flips robots to Allow | robots.txt.ts | mitigate | Default-deny exact-match (`hostname === PROD_HOST`); spoofed hosts get `Disallow: /` — fail-safe |
| Redirect loop (slash added then removed) | trailingSlash middleware | mitigate | Guard only ever ADDS a slash; loop structurally impossible |
</threat_model>

<verification>
1. `node scripts/gen-headers.mjs` exits 0 (parity gate untouched).
2. `npm run build` exits 0 AND `GITHUB_PAGES=true npm run build` exits 0.
3. Static dist: `dist/robots.txt` = deny variant; `dist/sitemap.xml` exists; `dist/sitemap-0.xml` has no `/brand/`; canonicals end with `/`; og:url == canonical; `– EVOLEA` titles; noindex meta present.
4. Deferred to phase verification (Cloudflare preview only, per Phase 1 precedent): `curl -sI https://<preview>/angebote` returns 301 with `location: /angebote/`; `curl -sI https://<preview>//evil.com` Location starts with a single `/` and same origin; `curl -s https://<preview>/robots.txt` returns `Disallow: /` with `X-Robots-Tag: noindex` on HTML responses; production robots.txt returns the Allow+Sitemap variant; `/sitemap.xml` returns 301 to `/sitemap-index.xml`; no redirect loop on any program page; Keystatic login and POST /api/csp-report still work.
</verification>

<success_criteria>
- SEO-01: hostname-keyed robots.txt endpoint exists with default-deny semantics and prerendered deny variant in static builds
- SEO-02: /sitemap.xml 301 redirect configured and rel=sitemap link in every head
- SEO-03: trailing-slash 301 middleware (with exemptions + open-redirect guard) and canonical/og:url/twitter:url unified on the trailing-slash form
- Both build modes green; gen-headers parity gate green
</success_criteria>

<output>
Create `.planning/phases/02-seo-structured-data/02-01-SUMMARY.md` when done.
</output>
