---
phase: 02-seo-structured-data
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/pages/blog/[...slug].astro
  - src/pages/en/blog/[...slug].astro
  - src/components/CafePage.astro
autonomous: true
requirements: [SEO-05, SEO-08]
tags: [seo, json-ld, blog, blogposting, event, structured-data]

must_haves:
  truths:
    - "Every blog post page (DE and EN) emits a BlogPosting JSON-LD with headline, description, author as Organization, publisher referencing the Organization @id, mainEntityOfPage = the trailing-slash canonical, and datePublished only when the post has a valid pubDate"
    - "Blog post pages emit og:type=article (via the Base ogType prop); all other pages keep og:type=website"
    - "Blog post pages emit a BreadcrumbList matching their visible inline breadcrumb nav (Startseite/Home → Blog → post title)"
    - "The EVOLEA Cafe page (DE + EN) emits exactly ONE Event JSON-LD with a computed future startDate (next 2nd Wednesday, 20:00 Zurich offset), eventSchedule, organizer @id, and a CITY-LEVEL PostalAddress only (no street address — deferred per locked decision D3)"
    - "No BreadcrumbList is emitted from CafePage (no visible breadcrumb exists there, per D3)"
  artifacts:
    - path: "src/pages/blog/[...slug].astro"
      provides: "BlogPosting + BreadcrumbList JSON-LD, ogType article"
      contains: "blogPostingSchema"
    - path: "src/pages/en/blog/[...slug].astro"
      provides: "EN BlogPosting + BreadcrumbList JSON-LD, ogType article"
      contains: "ogType=\"article\""
    - path: "src/components/CafePage.astro"
      provides: "Recurring Event JSON-LD"
      contains: "cafeEventSchema"
  key_links:
    - from: "src/pages/blog/[...slug].astro"
      to: "src/lib/seo.ts"
      via: "blogPostingSchema/breadcrumbSchema imports"
      pattern: "from '@/lib/seo'"
    - from: "src/components/CafePage.astro"
      to: "ORG_ID @graph anchor emitted by Base"
      via: "organizer: { '@id': ORG_ID } in cafeEventSchema"
      pattern: "#organization"
    - from: "blog templates"
      to: "src/layouts/Base.astro"
      via: "ogType=\"article\" prop (added in 02-01)"
      pattern: "ogType=\"article\""
---

<objective>
Emit BlogPosting JSON-LD (+ og:type=article + matching BreadcrumbList) from both blog detail templates and one recurring Event JSON-LD from the EVOLEA Cafe page — closing SEO-05 and SEO-08.

Purpose: BlogPosting with publisher → Organization @id makes blog posts pass the Rich Results test (roadmap criterion 3); the Cafe Event is the fourth Rich Results surface. The CMS cafe dates are stale (Jan–Mar 2025), so the startDate is computed, never read from CMS.
Output: Two modified blog templates, one modified CafePage component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-seo-structured-data/02-RESEARCH.md
@.planning/phases/02-seo-structured-data/02-PATTERNS.md
@src/pages/blog/[...slug].astro
@src/components/CafePage.astro

Locked decision D3 honored here: Event uses a city-level PostalAddress (addressLocality Zürich, addressCountry CH) — do NOT invent a street address; the street-address upgrade is a DEFERRED item (record it in the SUMMARY as deferred). No BreadcrumbList on CafePage (no visible breadcrumb there).

Blog templates are `export const prerender = true` — JSON-LD and og:type are baked at build time on BOTH builds; the trailing-slash middleware never sees these routes (platform 308s them, RESEARCH Pitfall 2). On the static fallback build the Event startDate freezes at build time — acceptable for the noindexed mirror (RESEARCH Pitfall 6).

Flag for the SUMMARY (content todo, NOT in scope to fix): the visible cafe dates in src/content/pages/evolea-cafe.json `daten[]` are stale (Jan–Mar 2025) and DE/EN rows disagree — needs a CMS content update by the user.
</context>

<interfaces>
From src/lib/seo.ts (built in 02-02 — use directly):
```typescript
export const ORG_ID = 'https://www.evolea.ch/#organization';
export function blogPostingSchema(opts: { headline: string; description: string; datePublished?: string; authorName: string; imageUrl?: string; canonicalUrl: string; lang: 'de' | 'en' }): Record<string, unknown>;
export function breadcrumbSchema(items: Array<{ label: string; href?: string }>, site: URL | undefined): Record<string, unknown>;
export function cafeEventSchema(opts: { lang: 'de' | 'en'; description: string }): Record<string, unknown>;
```
From src/components/JsonLd.astro: `<JsonLd schema={...} />`.
From src/layouts/Base.astro (02-01): `ogType?: 'website' | 'article'` prop, default 'website'.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BlogPosting + BreadcrumbList + ogType article in the DE blog template</name>
  <files>src/pages/blog/[...slug].astro</files>
  <read_first>src/pages/blog/[...slug].astro (entire file — the pubDate guard at ~lines 40-43 (`hasValidPubDate`), the `base` constant at line ~10, the Base invocation at ~line 63, and the inline breadcrumb nav at ~lines 68-74 whose labels the BreadcrumbList must mirror), .planning/phases/02-seo-structured-data/02-RESEARCH.md §Code Examples (BlogPosting)</read_first>
  <action>
    Import `JsonLd`, `blogPostingSchema`, `breadcrumbSchema` (path alias conventions as elsewhere). In frontmatter compute the trailing-slash canonical (`Astro.url.pathname` + '/' if missing, resolved against `Astro.site`). Build the BlogPosting via `blogPostingSchema`: headline = post.data.title, description = post.data.description, datePublished = `pubDate.toISOString()` ONLY when `hasValidPubDate` (reuse the existing guard — same gating as the visible `<time>` element), authorName = post.data.author (schema builder wraps it as Organization — 'EVOLEA Team' is not a person), imageUrl = `new URL(`${base}${post.data.image}`, Astro.site).href` only when post.data.image exists, canonicalUrl = canonical href, lang. Add `ogType="article"` to the `<Base ...>` invocation (SEO-05's og:type switch). Build breadcrumb items mirroring the visible nav exactly: 'Startseite' → translatePath('/'), 'Blog' → translatePath('/blog/'), current = post.data.title (no href). Render `<JsonLd schema={...} />` for both schemas adjacent to the nav (body placement valid).
  </action>
  <verify>
    <automated>npm run build && F=$(ls dist/blog/*/index.html | head -1) && grep -q '"@type":"BlogPosting"' "$F" && grep -q 'og:type" content="article"' "$F" && grep -q '"publisher":{"@id":"https://www.evolea.ch/#organization"}' "$F" && grep -q '"@type":"BreadcrumbList"' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - Every dist/blog/<slug>/index.html contains one BlogPosting (headline = post title, publisher @id = Organization, mainEntityOfPage ends with '/'), one BreadcrumbList (Startseite → Blog → title), and `og:type` content="article"
    - A post with a valid pubDate carries `datePublished`; the schema omits the key entirely for invalid dates (no 'Invalid Date' strings anywhere in dist)
    - `author` is an Organization node, not a Person
    - dist/index.html and dist/angebote/index.html still carry `og:type` content="website" (default untouched)
  </acceptance_criteria>
  <done>DE blog posts emit valid BlogPosting + matching breadcrumbs with article og:type.</done>
</task>

<task type="auto">
  <name>Task 2: Same treatment for the EN blog template</name>
  <files>src/pages/en/blog/[...slug].astro</files>
  <read_first>src/pages/en/blog/[...slug].astro (entire file — same structure as the DE template but uses getCollection('blogEn') and EN nav labels 'Home'/'Blog'; confirm its own pubDate guard and Base invocation lines), the just-edited src/pages/blog/[...slug].astro (replicate the pattern 1:1)</read_first>
  <action>
    Apply the identical changes to the EN template: imports, canonical computation, `blogPostingSchema` call with `lang: 'en'`, `ogType="article"` on Base, BreadcrumbList mirroring its visible nav ('Home' → translatePath('/'), 'Blog' → translatePath('/blog/') — use the exact label expressions the EN nav renders), both JsonLd emissions adjacent to the nav. Bilingual parity rule: this task must land in the same plan/commit window as Task 1.
  </action>
  <verify>
    <automated>npm run build && F=$(ls dist/en/blog/*/index.html | head -1) && grep -q '"@type":"BlogPosting"' "$F" && grep -q 'og:type" content="article"' "$F" && grep -q '"inLanguage":"en"' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - Every dist/en/blog/<slug>/index.html contains one BlogPosting with `"inLanguage":"en"`, one BreadcrumbList with EN labels, and og:type article
    - The DE and EN templates use the same schema-building pattern (a reviewer diffing the two frontmatter additions sees only label/collection/lang differences)
  </acceptance_criteria>
  <done>EN blog posts have full DE parity for BlogPosting/breadcrumbs/og:type.</done>
</task>

<task type="auto">
  <name>Task 3: Recurring Event JSON-LD on the EVOLEA Cafe page</name>
  <files>src/components/CafePage.astro</files>
  <read_first>src/components/CafePage.astro (props/getText/getArray conventions at lines 1-30; the custom cafe-hero section at ~line 37 — confirm there is NO breadcrumb nav, hence no BreadcrumbList here; locate where the page description text is computed), src/lib/seo.ts (cafeEventSchema + nextCafeDate from 02-02)</read_first>
  <action>
    In `src/components/CafePage.astro`: import `JsonLd` and `cafeEventSchema`. Compute the event description from the component's existing bilingual description expression (the same getText call/fallback that renders the intro copy). Render exactly one `<JsonLd schema={cafeEventSchema({ lang, description })} />` near the top of the component template (body placement valid). The builder (02-02) already enforces D3: single Event, computed future startDate via nextCafeDate(), eventSchedule (2nd Wednesday monthly, 20:00, Europe/Zurich), organizer @id, location Place 'EVOLEA' with city-level address only. Do NOT read the stale CMS `daten[]` rows for the schema and do NOT add any breadcrumb markup or BreadcrumbList. Record two items in the SUMMARY: (1) deferred — street-address upgrade pending user confirmation of the venue; (2) content todo — stale visible cafe dates in CMS need a user update.
  </action>
  <verify>
    <automated>npm run build && grep -q '"@type":"Event"' dist/angebote/evolea-cafe/index.html && grep -q '"addressLocality":"Zürich"' dist/angebote/evolea-cafe/index.html && ! grep -q '"streetAddress"' dist/angebote/evolea-cafe/index.html && ! grep -q '"@type":"BreadcrumbList"' dist/angebote/evolea-cafe/index.html</automated>
  </verify>
  <acceptance_criteria>
    - dist/angebote/evolea-cafe/index.html and dist/en/programs/evolea-cafe/index.html each contain exactly ONE `"@type":"Event"` JSON-LD
    - The Event's startDate matches `/^\d{4}-\d{2}-\d{2}T20:00:00\+0[12]:00$/`, is a future date, and its day-of-month is between 8 and 14 (a 2nd Wednesday)
    - location.address has addressLocality 'Zürich' and addressCountry 'CH' and NO streetAddress/postalCode keys (D3)
    - organizer is `{"@id":"https://www.evolea.ch/#organization"}`; eventSchedule present with byDay Wednesday, repeatFrequency P1M, scheduleTimezone Europe/Zurich
    - No BreadcrumbList anywhere in the cafe dist pages; `npm run build` AND `GITHUB_PAGES=true npm run build` exit 0
  </acceptance_criteria>
  <done>The cafe page emits one valid recurring Event with a computed future date and city-level venue; deferred street-address item recorded.</done>
</task>

</tasks>

<verification>
1. `npm run build` and `GITHUB_PAGES=true npm run build` both exit 0.
2. dist blog posts (DE + EN): BlogPosting + BreadcrumbList + og:type article, publisher/organizer @id resolution against the same-page @graph.
3. dist cafe pages: exactly one Event, city-level address, future 2nd-Wednesday startDate, no BreadcrumbList.
4. Deferred to phase verification (deployed URL): Google Rich Results test passes for a blog post (BlogPosting) and the cafe page (Event); watch Search Console for "event is in the past" warnings (RESEARCH Pitfall 6 warning sign).
</verification>

<success_criteria>
- SEO-05: BlogPosting JSON-LD with all required fields + og:type article on every blog post, both languages
- SEO-08: one recurring Event JSON-LD with eventSchedule and physical (city-level) venue address on the cafe page, both languages; street-address upgrade explicitly deferred (D3)
</success_criteria>

<output>
Create `.planning/phases/02-seo-structured-data/02-05-SUMMARY.md` when done.
</output>
