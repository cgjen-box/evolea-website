---
phase: 02-seo-structured-data
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/InnerPageHero.astro
  - src/components/programs/MiniGartenPage.astro
  - src/components/programs/MiniProjektePage.astro
  - src/components/programs/MiniTurnenPage.astro
  - src/components/programs/TagesschulePage.astro
  - src/components/programs/MiniAbenteuercampPage.astro
  - src/components/programs/MiniMuseumPage.astro
  - src/components/programs/MiniRestaurantPage.astro
autonomous: true
requirements: [SEO-06, SEO-07]
tags: [seo, json-ld, breadcrumbs, service, structured-data]

must_haves:
  truths:
    - "Every page that renders visible breadcrumbs via InnerPageHero (spenden, ueber-uns, kontakt, blog index, team, angebote index + EN twins) emits exactly one BreadcrumbList JSON-LD whose items match the visible breadcrumb labels and links"
    - "Each of the 7 program body components emits a BreadcrumbList JSON-LD mirroring its inline breadcrumb nav (same labels, same translatePath links, same order)"
    - "Each of the 7 program body components emits a Service JSON-LD with provider referencing the Organization @id, areaServed Zürich, and inLanguage matching the page language"
    - "BreadcrumbList items use absolute URLs built from the already-base-prefixed hrefs (no double base prefix on GitHub Pages)"
    - "No BreadcrumbList is emitted from CafePage (no visible breadcrumb there, per locked decision D3)"
  artifacts:
    - path: "src/components/InnerPageHero.astro"
      provides: "BreadcrumbList emission from the breadcrumbs prop"
      contains: "breadcrumbSchema"
    - path: "src/components/programs/MiniGartenPage.astro"
      provides: "BreadcrumbList + Service JSON-LD"
      contains: "serviceSchema"
    - path: "src/components/programs/MiniRestaurantPage.astro"
      provides: "BreadcrumbList + Service JSON-LD (deeper breadcrumb: includes Mini Projekte level)"
      contains: "breadcrumbSchema"
  key_links:
    - from: "src/components/InnerPageHero.astro"
      to: "src/lib/seo.ts"
      via: "breadcrumbSchema import"
      pattern: "from '@/lib/seo'"
    - from: "src/components/programs/*.astro"
      to: "ORG_ID @graph anchor emitted by Base"
      via: "provider: { '@id': ORG_ID } in serviceSchema"
      pattern: "#organization"
---

<objective>
Emit BreadcrumbList JSON-LD everywhere visible breadcrumbs exist (InnerPageHero consumers + the 7 program body components' inline navs) and Service JSON-LD on every Angebote program page — closing SEO-06 and SEO-07.

Purpose: BreadcrumbList is the supported rich result that makes the Angebote pages pass the Google Rich Results test (Service itself has NO Google rich result — RESEARCH Pitfall 8); Service still validates on validator.schema.org and strengthens entity understanding via the Organization @id link.
Output: 8 modified components; one InnerPageHero edit covers 11 consumer pages at once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-seo-structured-data/02-RESEARCH.md
@.planning/phases/02-seo-structured-data/02-PATTERNS.md
@src/components/InnerPageHero.astro
@src/components/programs/MiniGartenPage.astro

Key constraints from PATTERNS:
- InnerPageHero renders the breadcrumb nav TWICE (image layout ~lines 80-93, no-image layout ~lines 122-135) — emit ONE JsonLd, placed once outside the layout conditional.
- The `href` values in breadcrumbs/navs are already translatePath()-resolved AND base-prefixed — breadcrumbSchema absolutizes with `new URL(href, Astro.site)`; never prepend `${base}` again (Pitfall 7).
- SEO-06 mandates the JSON-LD items match the VISIBLE breadcrumbs — build the items array from the exact same expressions the nav renders (labels and hrefs), not from parallel constants.
- CafePage gets NO BreadcrumbList (D3) and is handled in plan 02-05 (Event) — do not touch it here.
- Body placement of JSON-LD is valid for Google (RESEARCH Pattern 3 notes).

Program audience ages for serviceSchema (from page descriptions / CLAUDE.md; omit audience when unknown):
Mini Garten 3–6, Mini Projekte 5–8, Mini Turnen 5–8, Mini Abenteuercamp 5–10, Mini Museum 5–8, Mini Restaurant 5–8, Tagesschule: omit minAge/maxAge (vision-stage program, no published ages).
</context>

<interfaces>
From src/lib/seo.ts (built in 02-02 — use directly, no exploration):
```typescript
export const ORG_ID = 'https://www.evolea.ch/#organization';
export function breadcrumbSchema(items: Array<{ label: string; href?: string }>, site: URL | undefined): Record<string, unknown>;
export function serviceSchema(opts: { name: string; description: string; url: string; lang: 'de' | 'en'; minAge?: number; maxAge?: number }): Record<string, unknown>;
```
From src/components/JsonLd.astro (built in 02-02):
```astro
<JsonLd schema={...} />  <!-- Props: { schema: Record<string, unknown> } -->
```
Canonical URL inside any component: slash-normalize `Astro.url.pathname` (append '/' if missing) and resolve against `Astro.site` — same two-line pattern Base.astro uses after 02-01.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BreadcrumbList emission in InnerPageHero</name>
  <files>src/components/InnerPageHero.astro</files>
  <read_first>src/components/InnerPageHero.astro (entire file — BreadcrumbItem interface at ~lines 17-30, the two duplicated breadcrumb navs, and where the layout conditional begins/ends), src/lib/seo.ts (breadcrumbSchema signature)</read_first>
  <action>
    Import `JsonLd` from `@components/JsonLd.astro` and `breadcrumbSchema` from `@/lib/seo`. Render `<JsonLd schema={breadcrumbSchema(breadcrumbs, Astro.site)} />` exactly ONCE, outside/above the image-vs-no-image layout conditional, guarded by `breadcrumbs.length > 0` if the prop can be empty. The existing `breadcrumbs` prop array IS the items array — pass it through unchanged (its `label`/`href` shape matches the builder contract; current page already omits href). Do not alter the visible nav markup or styles.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'BreadcrumbList' dist/spenden/index.html)" = "1" && test "$(grep -c 'BreadcrumbList' dist/en/donate/index.html)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/spenden/index.html` and `dist/en/donate/index.html` each contain exactly ONE `"@type":"BreadcrumbList"` (not two, despite the duplicated nav markup)
    - The ListItem names in dist match the visible breadcrumb labels on the same page (e.g. 'Startseite' then the page label on DE; 'Home' on EN)
    - ListItem `item` URLs are absolute and contain the base path exactly once (on the static build: one `/evolea-website` segment, never two)
    - The last ListItem (current page) has `name` but no `item`
  </acceptance_criteria>
  <done>All 11 InnerPageHero consumer pages emit one matching BreadcrumbList each, both languages.</done>
</task>

<task type="auto">
  <name>Task 2: Breadcrumb + Service JSON-LD in core program components</name>
  <files>src/components/programs/MiniGartenPage.astro, src/components/programs/MiniProjektePage.astro, src/components/programs/MiniTurnenPage.astro, src/components/programs/TagesschulePage.astro</files>
  <read_first>src/components/programs/MiniGartenPage.astro (props/getText convention lines 1-17 and the inline breadcrumb nav at ~lines 35-41 whose exact expressions the items array must reuse), .planning/phases/02-seo-structured-data/02-PATTERNS.md §programs section (per-file nav line numbers: MiniProjekte ~56, MiniTurnen ~38, Tagesschule ~34)</read_first>
  <action>
    In each of the four components: import `JsonLd` and `breadcrumbSchema`/`serviceSchema` (and use `Astro.site`/`Astro.url`, available in any component). Build the breadcrumb items array from the SAME expressions the inline nav renders — e.g. for MiniGartenPage: `[{ label: lang === 'de' ? 'Startseite' : 'Home', href: translatePath('/') }, { label: lang === 'de' ? 'Angebote' : 'Programs', href: translatePath('/angebote/') }, { label: hero.titel || 'Mini Garten' }]` — adjusting labels/fallbacks per component to mirror its own nav exactly (read each nav first; SEO-06 requires visible-markup parity). Compute the canonical url (slash-normalized pathname against Astro.site). Build the Service schema: name = the same expression as the breadcrumb current-page label; description = the component's existing hero subtitle/description expression (the same getText call that renders the hero text, with its existing fallback); url = canonical; lang = lang; ages per the table in context (Garten 3/6, Projekte 5/8, Turnen 5/8, Tagesschule omit). Render `<JsonLd schema={breadcrumbSchema(items, Astro.site)} />` and `<JsonLd schema={serviceSchema({...})} />` adjacent to the inline breadcrumb nav (body placement is valid).
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'BreadcrumbList' dist/angebote/mini-garten/index.html)" = "1" && grep -q '"@type":"Service"' dist/angebote/mini-garten/index.html && grep -q '"provider":{"@id":"https://www.evolea.ch/#organization"}' dist/angebote/mini-garten/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Each of dist/angebote/{mini-garten,mini-projekte,mini-turnen,tagesschule}/index.html contains exactly one BreadcrumbList and one Service JSON-LD; same for their EN twins (dist/en/programs/{mini-garden,mini-projects,mini-sports,day-school}/index.html)
    - Breadcrumb ListItem names equal the visible nav labels in the same rendered file (verify by grepping both the nav text and the JSON in one file per language)
    - Service.provider is `{"@id":"https://www.evolea.ch/#organization"}` (resolves against the @graph Base emits on the same page)
    - Mini Garten audience suggestedMinAge=3/suggestedMaxAge=6; Tagesschule has NO audience key
    - EN twins carry `"inLanguage":"en"` and EN labels ('Home', 'Programs')
  </acceptance_criteria>
  <done>Four program page pairs emit matching BreadcrumbList + Service JSON-LD.</done>
</task>

<task type="auto">
  <name>Task 3: Breadcrumb + Service JSON-LD in sub-program components</name>
  <files>src/components/programs/MiniAbenteuercampPage.astro, src/components/programs/MiniMuseumPage.astro, src/components/programs/MiniRestaurantPage.astro</files>
  <read_first>src/components/programs/MiniAbenteuercampPage.astro (nav ~line 57), src/components/programs/MiniMuseumPage.astro (nav ~line 57), src/components/programs/MiniRestaurantPage.astro (nav ~line 100 — NOTE: this component receives different props: { lang, translatePath, videoSrc, ... } with NO content prop; its breadcrumb may be one level deeper, including a Mini Projekte item — mirror whatever its nav actually renders)</read_first>
  <action>
    Same pattern as Task 2 for the three remaining components. Mirror each component's OWN inline nav exactly — MiniRestaurantPage's nav likely includes the intermediate 'Mini Projekte'/'Mini Projects' level (route is /angebote/mini-projekte/mini-restaurant/); the items array must include every visible level in order. For MiniRestaurantPage (no `content` prop) hardcode the Service name/description as bilingual ternaries consistent with the visible page copy ('Mini Restaurant', cooking project for children on the spectrum). Ages: Abenteuercamp 5/10, Museum 5/8, Restaurant 5/8. Render both JsonLd elements adjacent to each nav.
  </action>
  <verify>
    <automated>npm run build && grep -q '"@type":"Service"' dist/angebote/mini-projekte/mini-restaurant/index.html && test "$(grep -c 'BreadcrumbList' dist/angebote/mini-abenteuercamp/index.html)" = "1" && grep -q '"@type":"Service"' dist/angebote/mini-museum/index.html</automated>
  </verify>
  <acceptance_criteria>
    - dist pages for all three programs (DE + EN twins) contain exactly one BreadcrumbList and one Service each
    - MiniRestaurant's BreadcrumbList has one ListItem per visible nav level (verify count matches the rendered nav links + current page)
    - All Service nodes reference the Organization @id; `npm run build` AND `GITHUB_PAGES=true npm run build` exit 0
    - CafePage.astro is untouched by this plan (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>All 7 program components emit matching BreadcrumbList + Service; SEO-06 and SEO-07 complete.</done>
</task>

</tasks>

<verification>
1. `npm run build` and `GITHUB_PAGES=true npm run build` both exit 0.
2. Every InnerPageHero consumer page and every program page in dist contains exactly one BreadcrumbList whose names match its visible breadcrumb labels; every program page contains exactly one Service with provider @id = Organization.
3. No BreadcrumbList in dist/angebote/evolea-cafe/index.html (D3).
4. Deferred to phase verification: Google Rich Results test on a deployed Angebote page detects BreadcrumbList (the Service node is validated via validator.schema.org instead — Google has no Service rich result, RESEARCH Pitfall 8; document this split in the verification notes).
</verification>

<success_criteria>
- SEO-06: BreadcrumbList JSON-LD matches visible breadcrumbs on all InnerPageHero consumers and all 7 program components, DE + EN
- SEO-07: Service JSON-LD on all Angebote program pages with Organization @id provider linkage
</success_criteria>

<output>
Create `.planning/phases/02-seo-structured-data/02-04-SUMMARY.md` when done.
</output>
