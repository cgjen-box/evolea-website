---
phase: 03-performance-a11y-testing
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/layouts/Base.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/LogoWithSparkles.astro
  - src/components/InnerPageHero.astro
  - src/components/VideoHero.astro
  - src/components/CafePage.astro
  - src/components/programs/MiniGartenPage.astro
  - src/components/programs/MiniProjektePage.astro
  - src/components/programs/MiniTurnenPage.astro
  - src/components/programs/TagesschulePage.astro
  - src/components/programs/MiniAbenteuercampPage.astro
  - src/components/programs/MiniMuseumPage.astro
  - src/components/programs/MiniRestaurantPage.astro
  - src/components/brand/BrandPageBody.astro
  - src/pages/blog/index.astro
  - src/pages/en/blog/index.astro
  - src/pages/team/index.astro
  - src/pages/en/team/index.astro
  - src/pages/ueber-uns/index.astro
  - src/pages/en/about/index.astro
  - src/pages/spenden.astro
  - src/pages/en/donate.astro
autonomous: true
requirements: [PERF-02, PERF-03, A11Y-01]

must_haves:
  truths:
    - "Every <img> in src/ carries explicit width and height attributes (CLS fix)"
    - "Below-fold images use loading=lazy; the LCP hero element carries fetchpriority=high and is never lazy-loaded"
    - "The homepage video poster is preloaded with fetchpriority=high via a Base preloadImage prop (DE and EN)"
    - "The two Header nav landmarks and all 7 program breadcrumb navs carry distinguishing DE/EN-aware aria-labels"
    - "npm run build and GITHUB_PAGES=true npm run build both stay green"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Optional preloadImage prop emitting <link rel=preload as=image fetchpriority=high>"
      contains: "preloadImage"
    - path: "src/components/Header.astro"
      provides: "aria-labels on desktop (line ~99) and mobile (line ~206) navs"
      contains: "Hauptnavigation"
    - path: "src/components/programs/MiniGartenPage.astro"
      provides: "Labeled breadcrumb nav + LCP hero img with fetchpriority/width/height"
      contains: "fetchpriority"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/Base.astro"
      via: "preloadImage prop carrying the base-prefixed resolved poster path (caller prefixes, Base does not)"
      pattern: "preloadImage="
    - from: "src/components/programs/*.astro"
      to: "hero <img>"
      via: "fetchpriority=high + width/height, no loading attribute"
      pattern: "fetchpriority=\"high\""
---

<objective>
Give every `<img>` on the site explicit width/height (PERF-02), apply lazy/eager/fetchpriority policy per template (PERF-03: below-fold lazy, LCP hero fetchpriority="high" never lazy, homepage poster preloaded via a new Base `preloadImage` prop), and add DE/EN-aware aria-labels to the two Header navs plus the 7 program breadcrumb navs (A11Y-01).

Purpose: kills CLS, accelerates LCP, and resolves the axe landmark violation — three roadmap success criteria in one attribute pass.
Output: ~25 template files updated; zero `<img>` without dimensions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-a11y-testing/03-RESEARCH.md
@.planning/phases/03-performance-a11y-testing/03-PATTERNS.md

<interfaces>
From src/layouts/Base.astro (current Props — extend, do not restructure):
```typescript
interface Props {
  title: string;
  description?: string;
  image?: string;
  transparentHeader?: boolean;
  hideFooterCTA?: boolean;
  ogType?: 'website' | 'article';
}
```
Note: Base prefixes its own `image` og prop internally (`new URL(\`${base}${image}\`, Astro.site)`), but `preloadImage` must be passed ALREADY base-prefixed by the caller — do NOT re-prefix inside Base (locked decision 6 / PATTERNS).

Dimension constants (real pixel dims of the 03-01 converted .webp files — RESEARCH Pitfall 5 category defaults):
team 800×996 · program heroes 1200×670 · blog 1200×800 · logo 640×173 · poster 1920×943
Before using a constant, confirm against the actual file: `sips -g pixelWidth -g pixelHeight public/images/<file>.webp` — use the REAL dims when they differ (CSS object-cover wins for display; attributes only seed aspect-ratio).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Base preloadImage prop, homepage poster preload, Header nav labels + logo dims, Footer logo lazy</name>
  <files>src/layouts/Base.astro, src/pages/index.astro, src/pages/en/index.astro, src/components/Header.astro, src/components/LogoWithSparkles.astro, src/components/Footer.astro, src/components/VideoHero.astro</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "Base.astro preloadImage prop" + "Header.astro nav labels" + "<img> attribute work" sections (exact line numbers and analog markup)
    - src/layouts/Base.astro (Props interface, head), src/pages/index.astro lines ~75-85 (posterSrc expression), src/components/Header.astro lines 95-110 and 200-210, src/components/LogoWithSparkles.astro, src/components/Footer.astro lines 70-85, src/components/VideoHero.astro lines 35-50
  </read_first>
  <action>
    1. Base.astro: add `preloadImage?: string;` to the Props interface and destructuring (no default). In `<head>`, emit `{preloadImage && <link rel="preload" as="image" href={preloadImage} fetchpriority="high" />}`. Do NOT base-prefix inside Base — caller passes the prefixed path (per locked decision 6 / PATTERNS: unlike the og `image` prop which Base prefixes itself).
    2. index.astro + en/index.astro: pass `preloadImage={...}` on `<Base>` using the SAME resolved poster expression already computed for VideoHero's posterSrc at line ~80 (now `.webp` after 03-01): CMS `siteImages?.startseite?.videoPoster` with the hardcoded `${base}/images/hero-poster.webp`-style fallback. Keep both files' edits identical (DE/EN parity).
    3. Header.astro A11Y-01 (locked decision 3, exact strings): line ~99 desktop nav gets `aria-label={lang === 'de' ? 'Hauptnavigation' : 'Main navigation'}`; line ~206 mobile overlay nav gets `aria-label={lang === 'de' ? 'Mobile Navigation' : 'Mobile navigation'}`. Follow the existing bilingual aria-label analog at Header.astro:150.
    4. LogoWithSparkles.astro (~line 20, header logo img): add `width` and `height` matching the converted logo's real dims (default 640×173 — verify with sips). Stays eager (above-fold) — no loading attribute, no fetchpriority (poster preload owns LCP priority on the homepage).
    5. Footer.astro logo img (~lines 75-81): add `loading="lazy"` + the same logo width/height (below-fold).
    6. VideoHero.astro: leave the `<video>` and its `poster` attribute untouched (poster cannot take fetchpriority — the Base preload link covers it; never lazy the video). Verify-only: confirm no `<img>` inside VideoHero lacks dimensions.
  </action>
  <verify>
    <automated>grep -q 'preloadImage' src/layouts/Base.astro && grep -q 'preloadImage=' src/pages/index.astro && grep -q 'preloadImage=' src/pages/en/index.astro && grep -q 'Hauptnavigation' src/components/Header.astro && grep -q 'Mobile Navigation' src/components/Header.astro && grep -q 'loading="lazy"' src/components/Footer.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Base.astro head emits the preload link only when preloadImage is set; `fetchpriority="high"` present on the link
    - index.astro and en/index.astro both pass preloadImage with the CMS-or-fallback poster expression ending in .webp; the two files' new lines are identical
    - Header.astro contains both exact aria-label ternaries (Hauptnavigation/Main navigation, Mobile Navigation/Mobile navigation)
    - Header logo img has width+height and NO loading attribute; Footer logo img has loading="lazy" + width + height
    - VideoHero `<video poster=...>` unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Homepage LCP poster preloaded with high priority on DE+EN; both Header navs labeled bilingually; logo imgs dimensioned; footer logo lazy.</done>
</task>

<task type="auto">
  <name>Task 2: Program components + InnerPageHero — LCP hero img attributes and breadcrumb nav labels</name>
  <files>src/components/programs/MiniGartenPage.astro, src/components/programs/MiniProjektePage.astro, src/components/programs/MiniTurnenPage.astro, src/components/programs/TagesschulePage.astro, src/components/programs/MiniAbenteuercampPage.astro, src/components/programs/MiniMuseumPage.astro, src/components/programs/MiniRestaurantPage.astro, src/components/InnerPageHero.astro, src/components/CafePage.astro</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "Header + program breadcrumb nav labels" and "LCP hero img" sections (analog: InnerPageHero.astro:90 labeled nav; MiniGartenPage.astro:51 unlabeled nav, :86 hero img)
    - src/components/programs/MiniGartenPage.astro (the canonical shape — lines 7, 51, 86-90), src/components/InnerPageHero.astro lines 85-130, src/components/CafePage.astro
  </read_first>
  <action>
    For each of the 7 program body components (lang prop already in scope via `const { content, lang, translatePath, heroImage } = Astro.props` at ~line 7):
    1. Breadcrumb nav (e.g. MiniGartenPage.astro:51 `<nav class="mb-8 text-sm text-evolea-text-light">`): add `aria-label={lang === 'de' ? 'Brotkrumen' : 'Breadcrumb'}` (RESEARCH Pitfall 9 — required for a true 0-violation axe run; InnerPageHero already labels its own breadcrumb nav, leave it).
    2. Hero `<img>` (e.g. MiniGartenPage.astro:86-90): add `fetchpriority="high" width="1200" height="670"` (program-hero category constants — verify real dims of one converted hero with sips first; use real values if they differ). NO `loading` attribute (eager default — the LCP element must never be lazy). Keep existing classes/alt unchanged. MiniRestaurantPage's decorative `aria-hidden` img also gets width/height (PERF-02 covers every img) and `loading="lazy"` if below-fold.
    3. InnerPageHero.astro (~lines 120-125, optional heroImage img, already `loading="eager"`): add `fetchpriority="high"` + width/height (1200×670 category default).
    4. CafePage.astro: every `<img>` gets width/height; its hero-position img (rendered above the fold) gets `fetchpriority="high"` and no lazy; clearly below-fold imgs get `loading="lazy"`.
    Apply structurally identical edits across all 7 program components (they share the same shape).
  </action>
  <verify>
    <automated>test "$(grep -l 'Brotkrumen' src/components/programs/*.astro | wc -l | tr -d ' ')" = "7" && test "$(grep -l 'fetchpriority="high"' src/components/programs/*.astro | wc -l | tr -d ' ')" = "7" && grep -q 'fetchpriority="high"' src/components/InnerPageHero.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 7 program components contain the exact breadcrumb aria-label ternary (Brotkrumen/Breadcrumb) on the breadcrumb nav element
    - All 7 program hero imgs carry fetchpriority="high" + width + height and NO loading attribute
    - InnerPageHero heroImage img carries fetchpriority="high" + width + height and keeps loading="eager"
    - CafePage has zero `<img>` without width/height
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All program-page LCP heroes prioritized and dimensioned; third-nav landmark labeling complete (Header pair + 7 breadcrumbs + InnerPageHero's existing label = all nav landmarks distinguishable).</done>
</task>

<task type="auto">
  <name>Task 3: Site-wide below-fold img sweep + zero-missing-dimensions gate</name>
  <files>src/pages/blog/index.astro, src/pages/en/blog/index.astro, src/pages/team/index.astro, src/pages/en/team/index.astro, src/pages/ueber-uns/index.astro, src/pages/en/about/index.astro, src/pages/spenden.astro, src/pages/en/donate.astro, src/components/brand/BrandPageBody.astro</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "<img> attribute work" section (blog/index.astro:52 target-state analog)
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — Pattern 3 (per-template LCP/lazy policy) + Pitfall 5 (CSS-vs-attribute sizing)
    - Each file in the files list (the complete `<img>` census: 22 .astro files contain `<img>`; Tasks 1-2 covered the other 13)
  </read_first>
  <action>
    Sweep every remaining `<img>` in the 9 listed files (these complete the 22-file census from planning):
    - blog/index.astro + en/blog/index.astro card images: already `loading="lazy"` — add `width="1200" height="800"` (blog category).
    - team/index.astro + en/team/index.astro grids: keep/add `loading="lazy"`, add `width="800" height="996"` (team category; CMS-variable images use the category constant — containers fix both display dimensions).
    - ueber-uns/index.astro + en/about/index.astro (mission/about images): below-fold → `loading="lazy"` + real dims of the converted file.
    - spenden.astro + en/donate.astro (kind-schmetterlinge hero feeds InnerPageHero — already handled there; any in-body imgs like gianna portrait → lazy + dims).
    - BrandPageBody.astro (~12 imgs): all get width/height; below-fold get `loading="lazy"` (brand page is internal but PERF-02 says every img).
    Policy guard: never add `loading="lazy"` to an element that is the page's LCP candidate (heroes were handled in Tasks 1-2; everything in this task is below-fold or non-LCP).

    Final gate — write nothing new to the repo for this; run an inline multi-line-aware scan (grep -c per line is unreliable for multi-line tags): a node one-liner that reads every src/**/*.astro file, regex-extracts `<img\b[^>]*>` matches across newlines, and exits 1 listing any match missing `width=` or `height=`. Expect 0 missing. Then both builds green.
  </action>
  <verify>
    <automated>node -e "const{readFileSync,readdirSync,statSync}=require('fs');const{join}=require('path');const walk=d=>readdirSync(d).flatMap(f=>{const p=join(d,f);return statSync(p).isDirectory()?walk(p):p.endsWith('.astro')?[p]:[]});let bad=[];for(const f of walk('src')){const s=readFileSync(f,'utf8');for(const m of s.matchAll(/<img\b[^>]*>/gs)){if(!/width=/.test(m[0])||!/height=/.test(m[0]))bad.push(f+': '+m[0].slice(0,80))}}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('OK: all img tags dimensioned')" && GITHUB_PAGES=true npm run build && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The multi-line-aware node scan reports 0 `<img>` tags in src/ missing width= or height= (exit 0, prints OK line)
    - blog index cards: lazy + 1200×800; team grids: lazy + 800×996
    - No LCP hero element anywhere carries loading="lazy" (`grep -rn 'fetchpriority="high"' src/ | xargs -I{} true` — and `grep -rln 'loading="lazy"' src/components/programs/ src/components/InnerPageHero.astro` shows no hero img lines)
    - `npm run build` exit 0 AND `GITHUB_PAGES=true npm run build` exit 0
  </acceptance_criteria>
  <done>Zero undimensioned imgs site-wide; lazy/eager policy applied per template; both builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static attribute and aria-label edits on existing templates; no new input surfaces, endpoints, or dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Tampering (preload of attacker-controlled URL) | Base preloadImage prop | accept | Prop is only ever fed by repo-internal expressions (CMS string + hardcoded fallback), same trust level as the existing posterSrc; no user input reaches it |
</threat_model>

<verification>
- node multi-line img scan exits 0 across all of src/
- Header has 2 labeled navs; 7 program components have labeled breadcrumb navs
- Homepage head contains one preload link with fetchpriority="high" pointing at the .webp poster (DE and EN)
- Both build modes exit 0
</verification>

<success_criteria>
- Every `<img>` has explicit width/height (roadmap SC-1 second clause)
- Below-fold lazy; LCP elements fetchpriority="high", never lazy (roadmap SC-2)
- Nav landmark aria-labels in place, DE/EN aware (roadmap SC-3 first clause; axe 0-violation run asserted by 03-04)
- Both builds green
</success_criteria>

<output>
Create `.planning/phases/03-performance-a11y-testing/03-03-SUMMARY.md` when done. Record: total img tags dimensioned, any real-dims deviations from category constants.
</output>
