---
phase: 03-performance-a11y-testing
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/smoke.spec.ts
  - tests/a11y.spec.ts
  - tests/headers.spec.ts
  - tsconfig.json (only if astro check flags tests/)
  - public/videos/hero-mobile.mp4 (conditional — only if budget over AND SSIM gate passes)
autonomous: true
requirements: [HYG-04]

must_haves:
  truths:
    - "Playwright smoke suite passes: DE/EN homepage load, both contact forms render, donate language switcher works both directions, all program pages respond 200, 404 returns status 404 with branded bilingual content"
    - "Header-drift guard passes on one SSR route and one prerendered blog route against the real serving path (wrangler pages dev or TEST_BASE_URL staging)"
    - "axe-core reports 0 violations on the representative page set under reduced motion"
    - "Homepage total transfer weight is measured and the ≤1.5MB budget verdict is recorded (met, met-via-video-re-encode, or met-excluding-deferred-video) — PERF-01 budget clause closed"
    - "npm run build and GITHUB_PAGES=true npm run build both stay green"
  artifacts:
    - path: "playwright.config.ts"
      provides: "baseURL from TEST_BASE_URL || http://127.0.0.1:8788, channel chrome, wrangler webServer skipped when TEST_BASE_URL set"
      contains: "TEST_BASE_URL"
    - path: "tests/smoke.spec.ts"
      provides: "Route 200s, forms, donate switcher, 404, broken-image guard"
      min_lines: 60
    - path: "tests/a11y.spec.ts"
      provides: "AxeBuilder scans, reducedMotion, wcag2a/2aa/21a/21aa tags, violations == []"
      min_lines: 20
    - path: "tests/headers.spec.ts"
      provides: "Security-header drift guard on / and /blog/im-spektrum/"
      contains: "content-security-policy-report-only"
  key_links:
    - from: "tests/headers.spec.ts"
      to: "src/lib/security-headers.ts"
      via: "REQUIRED header map kept aligned with the single source constant"
      pattern: "strict-transport-security"
    - from: "playwright.config.ts"
      to: "dist/_worker.js + public/_headers"
      via: "webServer: npx wrangler pages dev dist --port 8788 (real workerd serving path)"
      pattern: "wrangler pages dev"
    - from: "package.json"
      to: "playwright.config.ts"
      via: "test:e2e npm script"
      pattern: "test:e2e"
---

<objective>
Stand up the permanent Playwright regression net (@playwright/test@1.57.0 exact + @axe-core/playwright, system Chrome, wrangler pages dev serving with TEST_BASE_URL staging fallback), run it green against the finished state of all three phases, then measure the homepage transfer weight and execute the locked budget decision path (conditional hero-video re-encode gated on SSIM ≥ 0.95).

Purpose: HYG-04 (smoke net + header-drift guard + axe 0-violations) and closure of the PERF-01 ≤1.5MB budget clause (measurement deliberately deferred here from 03-01 per locked decision 2: measure first, after image work, with the serving harness).
Output: playwright.config.ts + 3 spec files + test:e2e script + recorded budget verdict (+ possibly a re-encoded hero-mobile.mp4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-a11y-testing/03-RESEARCH.md
@.planning/phases/03-performance-a11y-testing/03-PATTERNS.md
@.planning/phases/02-seo-structured-data/02-VERIFICATION.md

<interfaces>
From src/lib/security-headers.ts: the single source constant for all security headers (gen-headers.mjs parses it as text). The headers.spec.ts REQUIRED map must assert at minimum (per RESEARCH Pattern 5):
- strict-transport-security: /max-age=31536000/
- x-content-type-options: /nosniff/
- x-frame-options: /DENY/
- referrer-policy: /strict-origin-when-cross-origin/
- permissions-policy: /./
- content-security-policy-report-only: /report-uri \/api\/csp-report/
NEVER assert ABSENCE of x-robots-tag (staging *.pages.dev intentionally sends noindex — suite must stay host-agnostic on it).

From src/i18n/utils.ts:62-78 (routeMappings): the 18 program paths — 9 DE under /angebote/ (incl. the /angebote/ index and mini-projekte/mini-restaurant) + 9 EN under /en/programs/ (incl. the /en/programs/ index).

Existing npm scripts (package.json): "build" = gen-headers + astro check + astro build; "build:cloudflare" = npm install @astrojs/cloudflare@12.6.13 --no-save + same. Homepage video: public/videos/hero-mobile.mp4 (1328KB on disk), referenced by VideoHero default and index.astro:79.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned test deps, playwright.config.ts, npm scripts, verify serving mode</name>
  <files>package.json, package-lock.json, playwright.config.ts</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — "Standard Stack" + "Package Legitimacy Audit" (all 4 packages slopcheck [OK]) + Pitfalls 6 and 7 + "playwright.config.ts skeleton"
    - package.json (current scripts + devDependencies), wrangler.toml (pages_build_output_dir already ./dist)
  </read_first>
  <action>
    1. Install (audit table in RESEARCH approves all — no checkpoint needed): `npm install --save-dev --save-exact @playwright/test@1.57.0` (EXACT pin — `^` resolves to 1.60.x and drags a second playwright-core into the tree, Pitfall 6; 1.57.0 matches the existing playwright-core devDep) and `npm install --save-dev @axe-core/playwright` (^4.11.3). wrangler is used via npx only — NOT added as a dependency.
    2. Create playwright.config.ts per the RESEARCH skeleton: `testDir: 'tests'`; `use: { baseURL: process.env.TEST_BASE_URL || 'http://127.0.0.1:8788', channel: 'chrome' }` (system Chrome — no browser downloads, repo precedent brand-qa.mjs); `webServer` set to `{ command: 'npx wrangler pages dev dist --port 8788', url: 'http://127.0.0.1:8788', reuseExistingServer: true, timeout: 120_000 }` but `undefined` when TEST_BASE_URL is set. Do NOT put the build inside webServer.command (Pitfall 7 — build:cloudflare re-installs the adapter every run); document the two-step flow in a config header comment: `npm run build:cloudflare` once, then `npm run test:e2e`.
    3. Add npm script `"test:e2e": "playwright test"` (locked decision 5).
    4. Verify the serving mode on THIS machine (macOS arm64): run `npm run build:cloudflare`, confirm `dist/_worker.js` exists, then `npx wrangler pages dev dist --port 8788` and `curl -sI http://127.0.0.1:8788/` returns 200 with `x-frame-options: DENY`. If wrangler cannot run locally (adapter/workerd constraint), the config already tolerates it: document in the config comment + SUMMARY that local runs use `TEST_BASE_URL=https://evolea-website.pages.dev npm run test:e2e` (or current branch preview URL) and which mode CI/local uses. No Cloudflare auth is needed for local pages dev; absent Keystatic env vars only affect /keystatic, which the suite never touches.
    5. If `astro check` (runs inside both builds and the pre-commit hook) flags playwright.config.ts or tests/: add an `exclude` for `tests` and `playwright.config.ts` to tsconfig.json rather than weakening strictness. Keep the new TS strict-clean (no `any`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.devDependencies['@playwright/test']!=='1.57.0')process.exit(1);if(!p.devDependencies['@axe-core/playwright'])process.exit(1);if(p.scripts['test:e2e']!=='playwright test')process.exit(1);console.log('deps+script ok')" && npx playwright --version && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies has `"@playwright/test": "1.57.0"` (no caret) and `"@axe-core/playwright"` present; wrangler NOT in dependencies/devDependencies
    - `npm ls playwright-core` shows a single version (1.57.0), not two
    - playwright.config.ts: baseURL honors TEST_BASE_URL, channel 'chrome', webServer skipped when TEST_BASE_URL set, header comment documents the two-step flow and the chosen local mode
    - Serving-mode verdict recorded: either `curl -sI http://127.0.0.1:8788/` shows 200 + x-frame-options after build:cloudflare, OR the TEST_BASE_URL fallback mode is documented as the local mode
    - `npm run build` exits 0 (astro check tolerates the new files)
  </acceptance_criteria>
  <done>Test infra installed and configured; single playwright-core in tree; serving mode for this machine determined and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write smoke.spec.ts, headers.spec.ts, a11y.spec.ts</name>
  <files>tests/smoke.spec.ts, tests/headers.spec.ts, tests/a11y.spec.ts</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — Patterns 4 and 5 + "Smoke route/selector inventory" (verified routes/selectors)
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — tests sections (brand-qa.mjs listener + interaction analogs)
    - src/i18n/utils.ts lines 62-78 (routeMappings — authoritative program path list), src/lib/security-headers.ts (header truth source), scripts/brand-qa.mjs lines 71-84 and 155-214 (listener + interaction patterns to translate)
  </read_first>
  <action>
    tests/smoke.spec.ts (locked decision 5 suite topology):
    - DE/EN homepage: goto `/` and `/en/` → response 200, `h1` visible.
    - Contact forms: `/kontakt/` and `/en/contact/` → `form[action^="https://formspree.io/f/"]` visible (selector verified at kontakt/index.astro:160).
    - Donate language switcher BOTH directions: goto `/spenden/`, click the LanguagePicker EN link, expect URL ending `/en/donate/`; then the reverse (per routeMappings).
    - All program pages 200: iterate the 18 paths from routeMappings (9 DE incl. /angebote/ + 9 EN incl. /en/programs/) — response 200 + `h1` visible. Import or mirror the path list; if mirrored, add a comment pointing at src/i18n/utils.ts:62-78 as source of truth.
    - 404: request `/this-page-does-not-exist/` → status 404, body contains "Seite nicht gefunden" AND "Page not found", both DE and EN link sets visible (hrefs /angebote/ and /en/programs/).
    - Broken-image guard (Pitfall 2): on `/`, `/en/`, `/angebote/`, `/blog/`, `/ueber-uns/` attach response/requestfailed listeners BEFORE goto and assert zero `/images/` requests with status ≥ 400 (translate brand-qa.mjs listener pattern into @playwright/test).

    tests/headers.spec.ts (Pattern 5): REQUIRED map per the interfaces block, asserted on `/` (SSR/middleware path) and `/blog/im-spektrum/` (prerendered/_headers path) via `request.get`. Both drift directions covered. Do NOT assert absence of x-robots-tag (host-agnostic rule). Also assert the 404 route (`/this-page-does-not-exist/`) carries x-frame-options DENY (closes T-3-04: headers on error paths).

    tests/a11y.spec.ts (Pattern 4): for each of `/`, `/en/`, `/angebote/mini-garten/` (green-badge page proving A11Y-02), `/kontakt/`, `/this-page-does-not-exist/`: `await page.emulateMedia({ reducedMotion: 'reduce' })` BEFORE goto (freezes animated prism gradients — deterministic contrast sampling, Pitfall 4), then `new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze()` and `expect(results.violations).toEqual([])`. If the installed @axe-core/playwright API shape differs slightly from the skeleton (RESEARCH A2), adapt at the call site — the assertion contract (0 violations, those tags, reduced motion) is fixed.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | grep -c 'spec.ts' | awk '{exit ($1>=3)?0:1}' && grep -q 'formspree.io' tests/smoke.spec.ts && grep -q "reducedMotion" tests/a11y.spec.ts && grep -q 'content-security-policy-report-only' tests/headers.spec.ts && ! grep -q 'x-robots-tag' tests/headers.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list` exits 0 and enumerates tests from all three spec files
    - smoke.spec.ts covers: 2 homepages, 2 contact forms, donate switcher both directions, 18 program paths, 404 (status 404 + both language strings + both link sets), broken-image guard on 5 pages
    - headers.spec.ts asserts the 6-header REQUIRED map on `/` AND `/blog/im-spektrum/` AND x-frame-options on the 404 route; contains NO assertion that x-robots-tag is absent
    - a11y.spec.ts scans exactly the 5 listed pages, sets reducedMotion before every goto, uses the 4 wcag tags, expects violations === []
    - No `any` types in the three specs (TS strict)
  </acceptance_criteria>
  <done>Three spec files written to the locked suite topology, listable by the runner.</done>
</task>

<task type="auto">
  <name>Task 3: Run suite green, measure homepage budget, execute conditional video decision, record verdict</name>
  <files>public/videos/hero-mobile.mp4 (conditional), .planning/phases/03-performance-a11y-testing/03-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — Pitfall 8 (budget math: video 1330KB + poster ~45KB + logo ~51KB + fonts ~96KB + unmeasured CSS/JS) + Open Question 1 (decision path) + Environment Availability (ffmpeg 8.0.1 installed)
    - playwright.config.ts (serving mode chosen in Task 1)
  </read_first>
  <action>
    1. Run the suite: `npm run build:cloudflare` then `npm run test:e2e` (or with TEST_BASE_URL per Task 1's mode). Fix any failures revealed (they assert finished state of Phases 1-3 — e.g., a missed image reference or a missing aria-label is a real defect to fix, not a test to weaken). Suite must exit 0.
    2. Measure homepage transfer weight (locked decision 2: MEASURE FIRST): with the same serving target, drive a page via the installed Playwright (one-off script or a non-CI-gating spec): viewport 1440×900, `goto('/', { waitUntil: 'load' })`, NO scrolling (lazy images correctly excluded), sum the byte sizes of all responses (use content-length / body size; for the streaming hero video count its full on-disk size, 1328KB, as the conservative figure). Budget threshold: 1.5MB = 1,536,000 bytes. Record the per-resource breakdown.
    3. Decision path (locked — do not deviate, do not ask):
       a. Total ≤ 1,536,000 bytes → budget MET. Done.
       b. Total over budget AND the overage is attributable to public/videos/hero-mobile.mp4 (i.e., total minus video ≤ budget) → pull PERF-V2-01 forward: re-encode with ffmpeg 8.0.1 (H.264, same resolution, `-movflags +faststart`, start at `-crf 28 -preset slow`, target ≤ ~700KB) to a temp file; compute SSIM vs the original (`ffmpeg -i candidate -i public/videos/hero-mobile.mp4 -lavfi ssim -f null -` → parse the `All:` score). Accept ONLY if SSIM All ≥ 0.95 — try crf 26 then 24 if under; if no candidate passes, KEEP the original video, record the budget as "met excluding deferred hero video" and PERF-V2-01 as still deferred. If a candidate passes: replace hero-mobile.mp4 IN PLACE (same filename — zero reference churn; hero.mp4 and the mini-restaurant videos are untouched), re-run the full suite, re-measure, record "met via video re-encode (SSIM=X.XXX)".
       c. Total over budget for any other reason → fix the actual cause (e.g., an unconverted image slipping through) and re-measure; do NOT touch the video for non-video overages.
    4. Purge note: flag in the SUMMARY that the Cloudflare zone cache must be purged after deploy (stale originals — existing /cloudflare skill command); do not deploy from this plan.
    5. Final gates: `npm run test:e2e` exit 0, `npm run build` exit 0, `GITHUB_PAGES=true npm run build` exit 0. Commit (pre-commit hook reruns build — expected).
  </action>
  <verify>
    <automated>npm run test:e2e && GITHUB_PAGES=true npm run build && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` exits 0 (all smoke + a11y + headers tests pass)
    - axe violations across the 5-page scan set: exactly 0
    - Measured homepage total bytes and per-resource breakdown recorded in the SUMMARY with one of three explicit verdicts: "met (N bytes)", "met via video re-encode (N bytes, SSIM=X.XXX ≥ 0.95)", or "met excluding deferred hero video (N bytes incl. video; N−video ≤ 1,536,000; PERF-V2-01 remains deferred)"
    - If re-encoded: `du -k public/videos/hero-mobile.mp4` ≤ ~700 and parsed SSIM All ≥ 0.95; original NOT replaced if SSIM gate failed
    - Both build modes exit 0
  </acceptance_criteria>
  <done>Suite green against the finished state of all phases; PERF-01 budget clause closed with a recorded, evidence-backed verdict; both builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | New test packages enter the dev toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering (supply chain) | @playwright/test, @axe-core/playwright installs | mitigate | RESEARCH Package Legitimacy Audit: slopcheck 0.6.1 [OK] on all 4 packages (Microsoft/Deque/Cloudflare/lovell maintainers, no [ASSUMED]/[SUS]); exact pin 1.57.0; no postinstall scripts on playwright packages (registry-verified); lockfile committed |
| T-3-06 | Information Disclosure | header-drift guard | mitigate | tests/headers.spec.ts permanently asserts the Phase 1 security-header set on SSR + prerendered + 404 paths — regression net against header loss |
</threat_model>

<verification>
- `npm run test:e2e` exit 0 (wrangler mode or documented TEST_BASE_URL mode)
- axe 0 violations on /, /en/, /angebote/mini-garten/, /kontakt/, 404
- Header map green on / and /blog/im-spektrum/; x-frame-options on 404
- Budget verdict recorded with numbers; video untouched unless SSIM ≥ 0.95
- Both builds green; single playwright-core in tree
</verification>

<success_criteria>
- Roadmap SC-5 fully satisfied (suite passes incl. header-drift guard; both builds green)
- Roadmap SC-3 axe clause proven (0 violations)
- Roadmap SC-1 budget clause closed with evidence (PERF-01 owned by 03-01; measurement clause executed here per locked decision 2)
</success_criteria>

<output>
Create `.planning/phases/03-performance-a11y-testing/03-04-SUMMARY.md` when done. Record: serving mode used (wrangler local vs TEST_BASE_URL), measured homepage bytes + breakdown, budget verdict, SSIM score if re-encoded, Cloudflare cache-purge reminder.
</output>
