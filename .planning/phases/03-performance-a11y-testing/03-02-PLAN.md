---
phase: 03-performance-a11y-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.mjs
  - src/styles/global.css
  - src/pages/404.astro
autonomous: true
requirements: [A11Y-02, A11Y-03]

must_haves:
  truths:
    - "evolea-green renders at >=4.5:1 contrast on its own /20 tint over cream on affected program pages (axe contrast violation resolved)"
    - "A branded, bilingual 404 page (prism hero, page closer, no emojis) links back to Angebote/Blog in BOTH German and English on one page"
    - "GET on an unknown path returns HTTP 404 with the branded page (SSR) and dist/404.html exists in the static build"
    - "npm run build and GITHUB_PAGES=true npm run build both stay green"
  artifacts:
    - path: "tailwind.config.mjs"
      provides: "Darkened green token (hex literal preserved for /10 /20 opacity modifiers)"
      contains: "#236247"
    - path: "src/styles/global.css"
      provides: "Mirrored CSS var --evolea-green"
      contains: "--evolea-green: #236247"
    - path: "src/pages/404.astro"
      provides: "Single bilingual 404 page per 03-UI-SPEC.md"
      min_lines: 80
  key_links:
    - from: "src/pages/404.astro"
      to: "src/components/InnerPageHero.astro"
      via: "prism gradient hero with empty breadcrumbs (suppresses BreadcrumbList JSON-LD)"
      pattern: "breadcrumbs=\\{\\[\\]\\}"
    - from: "src/pages/404.astro"
      to: "/angebote/ and /en/programs/"
      via: "base-prefixed hardcoded link cards (both language sets)"
      pattern: "\\$\\{base\\}/angebote/"
---

<objective>
Fix the evolea-green contrast violation by darkening the token in BOTH token files (#2D7A57 → #236247), and create the brand-compliant single bilingual 404 page exactly per 03-UI-SPEC.md.

Purpose: A11Y-02 (axe color-contrast → ≥4.5:1) and A11Y-03 (branded bilingual 404). Bonus verified win: routing 404s through 404.astro restores the Phase 1 security headers on error responses (missing today, verified live).
Output: Two-file token edit + src/pages/404.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-a11y-testing/03-UI-SPEC.md
@.planning/phases/03-performance-a11y-testing/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Darken evolea-green token in both token files</name>
  <files>tailwind.config.mjs, src/styles/global.css</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "tailwind.config.mjs + src/styles/global.css green token" section (exact current lines + consumers)
    - tailwind.config.mjs line 37 and src/styles/global.css line 37 (current values + inline comments)
  </read_first>
  <action>
    Replace `#2D7A57` with `#236247` in exactly two places (locked decision 3; verified ratios: 7.22:1 on white, 5.14:1 worst-case on its own /20 tint over cream, 6.54:1 on mint/20):
    1. tailwind.config.mjs:37 — `green: '#2D7A57'` → `green: '#236247'`. The value MUST remain a hex literal (memory rule: hex literals in tailwind config preserve `/10` `/20` opacity-modifier classes used at MiniGartenPage.astro:63/:153 and kontakt/index.astro:118-119). Keep the existing explanatory inline comment, updating it if it mentions the old hex.
    2. src/styles/global.css:37 — `--evolea-green: #2D7A57;` → `--evolea-green: #236247;`. Keep the comment style.
    Touch NOTHING else in either file (no token migration — explicitly out of scope per REQUIREMENTS.md). Note: `program-card-green` uses `border-evolea-teal`, not green — unaffected, do not edit.
  </action>
  <verify>
    <automated>grep -rni '2D7A57' src/ tailwind.config.mjs | wc -l | grep -qx '0' && grep -q "green: '#236247'" tailwind.config.mjs && grep -q -- '--evolea-green: #236247' src/styles/global.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni '2D7A57' src/ tailwind.config.mjs` returns 0 matches
    - tailwind.config.mjs contains `green: '#236247'` as a hex literal (not a CSS var reference)
    - global.css contains `--evolea-green: #236247;`
    - Exactly 2 lines changed across the 2 files (git diff stat)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both token files carry #236247; zero occurrences of the old hex anywhere; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Create the single bilingual 404 page per 03-UI-SPEC.md</name>
  <files>src/pages/404.astro</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-UI-SPEC.md — the COMPLETE contract: exact copy (German + English, final), layout structure, link targets, icon choices (sprout/school/book), color/contrast table, typography, spacing, accessibility notes
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "src/pages/404.astro" section (kontakt/index.astro analog, Base+InnerPageHero invocation, icon-link card markup analog)
    - src/pages/kontakt/index.astro — structural analog
    - src/components/InnerPageHero.astro — props interface (title, subtitle, label, breadcrumbs, size)
  </read_first>
  <action>
    Create src/pages/404.astro implementing 03-UI-SPEC.md exactly (locked decision 4). Non-negotiables from the spec:
    - SINGLE bilingual page: DE content first, EN block below wrapped in `<div lang="en">`. Do NOT create src/pages/en/404.astro (i18n fallback 302-strips /en/ before 404 renders — verified live).
    - Composition: Base (title="Seite nicht gefunden", description per spec meta table, NO hideFooterCTA — FooterDonationCTA is the mandatory page closer) + InnerPageHero (size="md", label="404", breadcrumbs={[]} which suppresses BreadcrumbList JSON-LD, title/subtitle exact spec copy) + content section.
    - Copy: use the spec's copy tables VERBATIM — hero H1 "Seite nicht gefunden", subtitle "Diese Seite ist weitergeflattert. Hier finden Sie schnell zurück.", DE intro + 3 links (Zur Startseite / Unsere Angebote / Zum Blog), EN H2 "Page not found" + intro + 3 links (Back to home / Our programs / To the blog).
    - Links: `const base = import.meta.env.BASE_URL.replace(/\/$/, '')`; DE set ${base}/, ${base}/angebote/, ${base}/blog/; EN set ${base}/en/, ${base}/en/programs/, ${base}/en/blog/. Do NOT use translatePath() for the EN set — hardcode both sets.
    - Link cards: whole card is one `<a>`, classes per spec (bg-white rounded-evolea shadow-soft p-6 flex flex-col items-center gap-3 text-center), `<Icon name=... size="lg" />` with sprout/school/book, labels in Fredoka 600 `text-evolea-purple-dark` (#8A3D7E — purple at ≤18px on white MUST be purple-dark, not #BA53AD), hover translateY(-4px) lift + shadow-card with a `prefers-reduced-motion: reduce` override, focus-visible 3px magenta outline offset 2px.
    - Grid: 1-col mobile → grid-cols-3 at md; section py-16 md:py-24, container max-w-4xl px-4; EN block in a white card p-6 md:p-8, mt-12 md:mt-16 separation.
    - NO `export const prerender` (on-demand SSR, auto-builds to dist/404.html in static mode). NO client JS. NO emojis (Icon.astro SVG only). NO `Astro.url` interpolation anywhere in the body (security: never reflect the requested path — copy is fully static). Exactly one H1 (hero) and one H2 (EN block).
  </action>
  <verify>
    <automated>grep -q 'breadcrumbs={\[\]}' src/pages/404.astro && grep -q 'Seite nicht gefunden' src/pages/404.astro && grep -q 'Page not found' src/pages/404.astro && grep -q 'lang="en"' src/pages/404.astro && grep -c 'Astro.url' src/pages/404.astro | grep -qx '0' && grep -c 'prerender' src/pages/404.astro | grep -qx '0' && GITHUB_PAGES=true npm run build && test -f dist/404.html && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/404.astro exists; src/pages/en/404.astro does NOT exist
    - Contains exact strings: "Seite nicht gefunden", "Diese Seite ist weitergeflattert. Hier finden Sie schnell zurück.", "Page not found", "This page has fluttered on."
    - Contains `breadcrumbs={[]}`, `lang="en"` wrapper, all 6 base-prefixed hrefs (`/angebote/`, `/blog/`, `/en/`, `/en/programs/`, `/en/blog/`)
    - 0 occurrences of `Astro.url`, `prerender`, `hideFooterCTA`, and emoji characters in the file
    - Icon names used: sprout, school, book (and no others for the cards)
    - `GITHUB_PAGES=true npm run build` exits 0 AND produces dist/404.html; `npm run build` exits 0
  </acceptance_criteria>
  <done>Branded bilingual 404 page exists per spec, builds to 404.html in static mode, both builds green. (HTTP 404 status + axe scan asserted by plan 03-04's suite.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→404 renderer | Arbitrary unmatched URLs reach the 404 route (untrusted path input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering/XSS (reflected path) | src/pages/404.astro | mitigate | Fully static copy; zero `Astro.url`/query interpolation in the body (grep-gated in acceptance criteria) |
| T-3-04 | Information Disclosure (headerless error responses) | 404 response path | mitigate | 404.astro routes through sequence() middleware → Phase 1 security headers now present on 404s (verified missing today; asserted by 03-04 suite) |
</threat_model>

<verification>
- Old hex absent repo-wide; new hex in both token files
- 404.astro passes all string/structure grep gates; dist/404.html produced in static build
- Both build modes exit 0
- Visual check (executor, dev server): 404 page at 375px — cards stack 1-col, gradient hero renders, gold CTA appears once (via page closer only)
</verification>

<success_criteria>
- evolea-green ≥4.5:1 on affected program pages (token verified math: worst case 5.14:1)
- Branded bilingual 404 with Angebote/Blog links in both languages, brand commandments 1/3/7/8/9 satisfied
- Both builds green
</success_criteria>

<output>
Create `.planning/phases/03-performance-a11y-testing/03-02-SUMMARY.md` when done.
</output>
