---
phase: 03-performance-a11y-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert-images.mjs
  - package.json
  - public/images/** (WebP conversions + original deletions)
  - src/content/settings/images.json
  - src/content/team/*.json
  - src/content/pages/spenden.json
  - src/content/pages/about.json
  - src/content/blog/*.mdx
  - src/content/blogEn/*.mdx
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/brand/BrandPageBody.astro
  - src/lib/seo.ts
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/pages/angebote/**/index.astro
  - src/pages/en/programs/**/index.astro
  - src/pages/spenden.astro
  - src/pages/en/donate.astro
  - src/pages/ueber-uns/index.astro
  - src/pages/en/about/index.astro
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Team, program-hero, blog, and logo images are served as WebP at display size (sRGB preserved) with no visible photo degradation"
    - "No page references a converted-away .png/.jpg — zero dangling image references, zero orphan .webp files (two-way)"
    - "og:image continues to resolve to a JPEG/PNG wherever a template feeds it (social-scraper compatibility)"
    - "npm run build and GITHUB_PAGES=true npm run build both stay green"
  artifacts:
    - path: "scripts/convert-images.mjs"
      provides: "Committed manifest-driven sharp conversion script with two-way grep gate"
      min_lines: 60
    - path: "public/images/hero-poster.webp"
      provides: "Converted homepage poster (1920w)"
    - path: "public/images/logo/evolea-logo-new.webp"
      provides: "Converted logo with alpha"
    - path: "src/content/settings/images.json"
      provides: "CMS hero paths pointing at .webp"
      contains: ".webp"
  key_links:
    - from: "src/content/settings/images.json"
      to: "public/images/programs/*.webp"
      via: "CMS path strings (templates add base prefix)"
      pattern: "\\.webp"
    - from: "src/pages/angebote/mini-garten/index.astro"
      to: "public/images/programs/mini-garten-hero.webp"
      via: "hardcoded fallback string (flipped in same commit as CMS value)"
      pattern: "mini-garten-hero\\.webp"
    - from: "src/lib/seo.ts"
      to: "public/images/logo/evolea-logo-new.webp"
      via: "JSON-LD logo URL (line ~93)"
      pattern: "evolea-logo-new\\.webp"
---

<objective>
Convert every referenced team/program-hero/blog/logo image in `public/images/` to WebP at display size IN PLACE (locked decision: no src/assets migration), flip all reference surfaces (CMS JSON + MDX frontmatter + hardcoded fallbacks) in one commit, then delete converted originals after a two-way grep gate — keeping any original that feeds og:image.

Purpose: PERF-01 — ~18MB of referenced PNG/JPG shrinks to ~1.2MB of WebP; the single largest performance lever in the phase.
Output: scripts/convert-images.mjs (committed), ~25 .webp assets, all reference surfaces flipped, originals deleted, both builds green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-a11y-testing/03-RESEARCH.md
@.planning/phases/03-performance-a11y-testing/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run the manifest-driven sharp conversion script (additive — originals retained)</name>
  <files>scripts/convert-images.mjs, package.json, public/images/** (new .webp files)</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — "Runtime State Inventory" table (full reference surface) + "Pattern 1" (manifest snippet) + "Common Pitfalls" 2 and 5
    - scripts/gen-headers.mjs — the repo's canonical node tooling style (shebang, JSDoc header, node: imports, ROOT resolution, failure-array + exit codes)
    - src/content/settings/images.json, src/content/team/*.json, src/content/pages/spenden.json, src/content/pages/about.json — enumerate referenced paths for the manifest
  </read_first>
  <action>
    Create scripts/convert-images.mjs following the gen-headers.mjs tooling style (shebang, multi-line JSDoc explaining purpose + `node scripts/convert-images.mjs` invocation, `node:`-prefixed imports, `ROOT = resolve(__dirname, '..')` path resolution, failure accumulation array, exit 1 on any failure / exit 0 with one OK line).

    Build the MANIFEST as an explicit array of `{ src, width }` — NO globbing (per RESEARCH: public/images/ contains ~6MB of verified orphans plus the untracked generated/ dir). Derive the entry list by grepping every `.png`/`.jpg`/`.jpeg` path referenced from `src/**` (`grep -rho '/images/[^"'\'' ]*\.\(png\|jpe\?g\)' src/`), de-duplicating, and EXCLUDING:
    - public/images/og-default.jpg (social-scraper og:image — never convert)
    - apple-touch-icon.png, favicon assets, qr-code.svg
    - already-.webp blog assets (buchtipp-anders-nicht-falsch.webp, spektrum-kreis.webp)
    - public/images/generated/ (untracked), public/videos/
    - the broken reference /images/about/children-playing-1.jpg (file does not exist — handled in Task 2)
    Unreferenced orphans (projects.jpg, sports.png, school.jpg, garden.jpg, tagesschule.jpg, hero-main.jpg, homepage-hero.png) are NOT converted and NOT deleted — out of scope; list them in a script comment.

    Target widths per RESEARCH category constants: team photos 800, program/inner-page heroes 1200, blog images 1200, hero-poster.jpg 1920, logo/evolea-logo-new.png 640. Pipeline: `sharp(src).resize({ width, withoutEnlargement: true }).webp({ quality: 82 }).toFile(out)` where `out` swaps the extension to .webp. sharp 0.34.5 is already in node_modules via Astro — import it directly, install nothing.

    Logo discretion (RESEARCH Open Question 3): if evolea-logo-new.webp at 640w/q82 exceeds 60KB, retry at quality 75; keep whichever is smaller while remaining visually clean at 2x header size (~112px tall).

    After conversion, the script verifies each output: file exists, is smaller than its source, and `sharp(out).metadata()` reports `space: 'srgb'` (all sources verified sRGB already — flag any deviation as a failure).

    Add npm script `"convert:images": "node scripts/convert-images.mjs"` to package.json (do NOT chain into build — one-off tooling, mirrors the gen:headers alias style).

    Run the script. Originals are NOT deleted in this task (deletion happens in Task 3 after the reference flip, so every intermediate commit keeps the site rendering).
  </action>
  <verify>
    <automated>node scripts/convert-images.mjs && ls public/images/hero-poster.webp public/images/logo/evolea-logo-new.webp && node -e "const s=require('sharp');s('public/images/hero-poster.webp').metadata().then(m=>{if(m.space!=='srgb')process.exit(1);console.log('srgb ok',m.width)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/convert-images.mjs` exits 0 and prints one OK summary line
    - A .webp sibling exists for every manifest entry; every .webp is byte-smaller than its source (script-enforced)
    - `node -e` sharp metadata check on hero-poster.webp and one team photo reports `space: 'srgb'`; logo .webp reports 4 channels (alpha preserved)
    - Spot sizes within RESEARCH-probed ballpark: team photo .webp ≤ 120KB, hero-poster.webp ≤ 80KB, logo .webp ≤ 60KB
    - package.json contains `"convert:images": "node scripts/convert-images.mjs"`
    - No original .png/.jpg deleted yet (`git status` shows only additions + package.json/script changes)
  </acceptance_criteria>
  <done>Committed conversion script + all .webp outputs exist alongside retained originals; script self-verifies sRGB and size reduction.</done>
</task>

<task type="auto">
  <name>Task 2: Flip ALL image references — CMS JSON, MDX frontmatter, and hardcoded fallbacks in one commit</name>
  <files>src/content/settings/images.json, src/content/team/*.json, src/content/pages/spenden.json, src/content/pages/about.json, src/content/blog/*.mdx, src/content/blogEn/*.mdx, src/components/Header.astro, src/components/Footer.astro, src/components/brand/BrandPageBody.astro, src/lib/seo.ts, src/pages/index.astro, src/pages/en/index.astro, src/pages/angebote/**/index.astro, src/pages/en/programs/**/index.astro, src/pages/spenden.astro, src/pages/en/donate.astro, src/pages/ueber-uns/index.astro, src/pages/en/about/index.astro</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-PATTERNS.md — "Image reference updates" section (Analogs A/B/C with exact line numbers)
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — "Runtime State Inventory" table + Open Question 2 (og:image check)
    - src/layouts/Base.astro — confirm the `image` prop default stays `/images/og-default.jpg` and identify which callers (if any) pass `image=` with a converted path
  </read_first>
  <action>
    Flip every old-extension reference to .webp in a SINGLE commit (locked decision: CMS JSON value + hardcoded fallback for the same image must change together). Surfaces, per the RESEARCH inventory:

    1. CMS JSON (edit string values in place, root-relative paths WITHOUT base prefix — templates add `base +`; only edit non-null values): src/content/settings/images.json (8 paths: 6 programmeHeroes + evolea-cafe-hero + kind-schmetterlinge + evolea-schloss + videoPoster if present), src/content/team/*.json (4 photo fields), src/content/pages/spenden.json (gianna-spiess), src/content/pages/about.json (mission bild).
    2. Blog MDX frontmatter: ~18 `image:` entries across src/content/blog/*.mdx + src/content/blogEn/*.mdx — match the exact shape of the already-converted buchtipp-anders-nicht-falsch.mdx line 6 (`image: "/images/blog/<name>.webp"`). Skip the 2 already-.webp entries.
    3. Hardcoded source fallbacks (same commit): Header.astro:38 + Footer.astro:31 (logo), src/lib/seo.ts:93 (JSON-LD logo URL), src/pages/index.astro:80 + src/pages/en/index.astro:80 (hero-poster fallback), all 18 program wrapper pages (heroImage fallback strings, e.g. mini-garten/index.astro:17-19), angebote/index.astro + en/programs/index.astro (evolea-schloss), evolea-cafe pages x2, mini-projekte/mini-restaurant pages x2 (poster), spenden.astro + en/donate.astro (kind-schmetterlinge + gianna fallback), ueber-uns + en/about (mission fallback), BrandPageBody.astro (~12 refs at lines ~454/457/510/519 and others — grep the file).
    4. Fix the pre-existing broken fallback: `/images/about/children-playing-1.jpg` in ueber-uns + en/about points at a nonexistent file — repoint to the converted `/images/about/children-playing-2.webp`.

    og:image guard (RESEARCH Open Question 2): before flipping, grep all `<Base` invocations for an `image=` prop. Base's default `/images/og-default.jpg` stays untouched (excluded from manifest). If any caller passes a converted image path to Base's `image` prop, do NOT flip that one reference — leave it on the original extension and record the file in the Task 3 keep-list. (Research expects none — blog [...slug].astro uses post images only for JSON-LD, where WebP is fine.)

    Preserve DE/EN wrapper byte-parity where it exists today (program wrapper pairs are byte-identical thin wrappers — apply identical edits to both halves of each pair).
  </action>
  <verify>
    <automated>grep -rn 'mini-garten-hero\.webp' src/content/settings/images.json src/pages/angebote/mini-garten/index.astro | grep -c webp | grep -q 2 && grep -c '\.webp' src/content/settings/images.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - For each converted image, BOTH its CMS JSON value (where one exists) and its hardcoded fallback reference .webp — verified pairwise for the 6 programmeHeroes (grep each hero name returns .webp in both images.json and its wrapper)
    - `grep -rn 'image:' src/content/blog/ src/content/blogEn/` shows zero `.png`/`.jpg`/`.jpeg` frontmatter values
    - `grep -n 'evolea-logo-new' src/components/Header.astro src/components/Footer.astro src/lib/seo.ts` returns .webp on all three
    - `grep -rn 'children-playing-1' src/` returns 0 matches; ueber-uns + en/about fallback points at children-playing-2.webp
    - Base.astro default og image still `/images/og-default.jpg`; og-default.jpg untouched on disk
    - `npm run build` exits 0
    - All reference flips land in ONE commit
  </acceptance_criteria>
  <done>Every reference surface (CMS JSON, MDX, hardcoded fallbacks, seo.ts) points at .webp; broken children-playing-1 fallback fixed; build green; single commit.</done>
</task>

<task type="auto">
  <name>Task 3: Two-way grep gate, delete converted originals, both builds green</name>
  <files>public/images/** (git rm of converted originals)</files>
  <read_first>
    - .planning/phases/03-performance-a11y-testing/03-RESEARCH.md — "Canonical check after rename" (two-way gate command) + Pitfall 2
    - scripts/convert-images.mjs — manifest = authoritative list of what was converted (and therefore what may be deleted)
  </read_first>
  <action>
    Run the two-way gate, then delete originals:

    Gate A (no dangling old refs): `grep -rn '\.\(png\|jpe\?g\)' src/ --include='*.astro' --include='*.ts' --include='*.json' --include='*.mdx'` must return ONLY the documented allowlist: og-default.jpg (Base.astro default), apple-touch-icon.png (Base head), any og-feeder original kept per Task 2's guard, and non-image incidental matches (none expected). Anything else = fix before proceeding.

    Gate B (no orphan webp): every .webp created by the manifest must be referenced from src/ — for each manifest output, `grep -rq '<basename>.webp' src/` succeeds. Add this two-way check to the END of scripts/convert-images.mjs as a `--verify` mode (or a verification block), reusing the gen-headers.mjs failure-array pattern, so the gate is re-runnable.

    Deletion: `git rm` every ORIGINAL whose manifest entry was converted AND whose reference was flipped, EXCEPT (a) og-default.jpg, (b) any og-feeder original identified in Task 2, (c) the unreferenced orphans (never converted — leave untouched, out of scope per locked decision 1). Use `git rm` (not plain rm) so deletions are staged and tracked.

    Then prove both build modes: `npm run build` and `GITHUB_PAGES=true npm run build` both exit 0. Commit (the pre-commit hook re-runs the build — expected).

    Note for SUMMARY: homepage ≤1.5MB budget measurement is intentionally deferred to plan 03-04 (locked decision 2: measure first AFTER image work, with the wrangler/Playwright harness; conditional video re-encode lives there).
  </action>
  <verify>
    <automated>grep -rn '\.\(png\|jpe\?g\)' src/ --include='*.astro' --include='*.ts' --include='*.json' --include='*.mdx' | grep -v 'og-default\.jpg' | grep -v 'apple-touch-icon\.png' | wc -l | grep -qx '0' && GITHUB_PAGES=true npm run build && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Gate A: grep for `.png|.jpg|.jpeg` across src/ returns only og-default.jpg, apple-touch-icon.png, and documented og-feeder keeps — nothing else (filtered count = 0)
    - Gate B: every manifest .webp output has ≥1 reference in src/ (script `--verify` exits 0)
    - `git ls-files` no longer lists any converted original (spot-check: `git ls-files public/images/team/ | grep -c '\.png$'` = 0)
    - og-default.jpg still tracked and on disk
    - `npm run build` exit 0 AND `GITHUB_PAGES=true npm run build` exit 0
  </acceptance_criteria>
  <done>Two-way gate green, converted originals removed from git, og-feeders kept, both build modes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Asset/content rename phase; no new endpoints, no new input surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (supply chain) | sharp import in convert-images.mjs | accept | sharp 0.34.5 already resolved in lockfile via Astro; slopcheck [OK] per RESEARCH Package Legitimacy Audit; no new install |
| T-3-02 | Denial of Service (broken assets) | dangling image refs after rename | mitigate | Two-way grep gate in Task 3 + manifest-only conversion (no globbing); plan 03-04 smoke suite adds runtime broken-image guard |
</threat_model>

<verification>
- `node scripts/convert-images.mjs` idempotent-safe and exits 0; `--verify` gate exits 0
- Gate A filtered grep count = 0; Gate B all .webp referenced
- Both build modes exit 0
- Visual spot-check (executor): open one team .webp and one program hero .webp at display size — no visible degradation vs original (brand-critical photography)
</verification>

<success_criteria>
- All referenced team/program/blog/logo images served as WebP at display size, sRGB preserved
- Zero dangling .png/.jpg references outside the allowlist; zero orphan .webp
- og:image surfaces still JPEG/PNG
- Both builds green; reference flips landed in one commit
</success_criteria>

<output>
Create `.planning/phases/03-performance-a11y-testing/03-01-SUMMARY.md` when done. Record: final manifest size, per-category before/after KB, any og-feeder originals kept, logo quality choice (q82 vs q75).
</output>
